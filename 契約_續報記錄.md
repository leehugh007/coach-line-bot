# 契約 — 續報記錄

> **版本**：v1（三輪 yi-challenge 終審通過定版）
> **最後更新**：2026-04-20
> **作者**：Claude（休校長小幫手 session，經三輪 yi-challenge 雙 agent 挑戰 + 歷史踩坑比對）
> **狀態**：定版，可進 Phase 0-4 實作

## 變更日誌

- **v0 草案**：初版介面契約骨架
- **v1-review（第一輪後）**：修 12 個 🔴（生命週期 / 命名 / 邏輯矛盾 / 值域 fallback）+ 7 個 ⚠️
- **v1-final（第二輪後）**：修 4 個 🔴（Redis miss window / T7 缺 SELECT / 降級漏 pair / label 矛盾）+ 4 個 ⚠️
- **v1（第三輪後，本版）**：修 4 個 🔴（contextSuffix if/else 互斥 / INTENT_TO_LABEL 單一映射 / Phase 2 子步驟順序 / T2 動作順序標號）+ race 條目入 §7

## 0. 一頁摘要

**解決什麼**：結業期學員表達「想續／已續／再想想」的意向，現在只有 QR 固定選項那條路會通知教練+記 Redis TTL，自由文字全靠 6 關鍵字掃描；Supabase **沒有任何永久欄位**，跨 session 失憶。

**怎麼解**（零 AI，全靠 QR 固定文字 + 自由文字 6 關鍵字兜底）：
1. 擴大 QR 觸發時機（第 10 週五起推一次，現在只有第 11 週五 + 第 12 週四）
2. QR 點選 → 寫 Supabase `users.renewal_intent` + `renewal_intent_at` + `renewal_intent_source`
3. `notifyRenewalInterest` 擴 evidence + 48hr cooldown 去重（**降級強制 push 豁免**）
4. SYSTEM_PROMPT contextSuffix 優先序：`renewal_confirmed_at != null`（跨所有狀態）→ `renewal_intent='interested'` → 既有 `alreadyRenewed`（兜底）→ `alreadyMentioned` → first-time
5. 自由文字 6 關鍵字 fallback 路保留（不擴、不動），**加 classStatus gate + 已有 intent 就不覆蓋**
6. 教練後台（admin/students）新增手動勾選 `renewal_confirmed_at`（業務真相）

**關鍵設計**：
- **「意向（intent）」vs「事實（confirmed）」分兩欄**：intent 靠 QR/關鍵字推測，confirmed 只能教練手動勾
- **Redis profile 一律 camelCase，DB 一律 snake_case，restore/sync 層做轉換**（防場景 1 事故復活）
- **renewal_intent 生命週期明寫**：memberLeft / 重加入班級 → 清；只 QR 降級豁免 cooldown
- **enum 只 3 值**（刪 maybe）：`interested / thinking / not_interested`

---

## 1. 狀態機總圖

### 狀態空間（組合自兩個欄位）

```
renewal_intent        ∈ { NULL, "interested", "thinking", "not_interested" }   ← 3 值 + NULL
renewal_confirmed_at  ∈ { NULL, <timestamp> }
```

### 語意分類

| 類別 | intent | confirmed_at | 意義 | SYSTEM_PROMPT 行為 |
|------|--------|--------------|------|------|
| **未表態** | NULL | NULL | 沒問過、沒回答 | 照現有 alreadyMentioned / alreadyRenewed 邏輯 |
| **有意向未確認** | interested/thinking/not_interested | NULL | QR/關鍵字捕捉 | `interested`→不再提；其他→照既有邏輯（首次自然帶，TTL 當日限 1） |
| **已確認續報** | 任一 | NOT NULL | 教練後台勾過 | **永遠**不再提續報（跨所有 classStatus） |

### 狀態轉換圖

```
          [NULL, NULL]
              │
              │ 第 10/11/12 週 QR 推播 → 學員點選
              │ 或 自由文字 6 關鍵字命中
              ▼
  ┌──────────────────────────┐
  │ intent = MAP[text]        │ (受 gate 限制，詳 §5)
  │ intent_at = now()         │
  │ intent_source = ...       │
  └──────────────────────────┘
              │
              ├─ interested ─────────▶ push 教練+助教（cooldown 48hr 內去重）
              ├─ thinking ───────────▶ push 教練+助教（cooldown）
              └─ not_interested ─────▶ push 教練+助教（cooldown）
                                         │
                                         │ 降級 interested → not_interested
                                         ▼ (強制豁免 cooldown，必 push)
                                     push 教練+助教

           （intent 可被後續 QR 點擊 overwrite；關鍵字 T3 已有 intent 則不覆蓋）
              │
              │ memberLeft / 重加入班級 → 清 intent 欄位（保留 confirmed_at）
              ▼
          [NULL, ...]
              │
              │ 教練後台勾選「已續報」
              ▼
  ┌──────────────────────────┐
  │ confirmed_at = now()      │ (只教練後台可寫)
  └──────────────────────────┘
              │
              ▼
     SYSTEM_PROMPT 永遠不提續報（優先級最高，跨所有 classStatus）
```

### 不可逆規則

- `renewal_confirmed_at` 一旦 set 就不再被 Bot 自動清除（只允許教練後台手動清）
- `renewal_intent` 可被新 QR 點擊 overwrite（包含降級）；**memberLeft / 重加入班級強制清除**
- 自由文字關鍵字 T3 寫入有條件：**`renewal_intent IS NULL` 才寫**（QR 永遠優於關鍵字）

---

## 2. 欄位字典（Field Contract）

### 新增欄位（Supabase `public.users` 表，snake_case）

| 欄位 | 型別 | 值域（完整枚舉） | 由誰填 | 什麼時候填 | 消費者 |
|------|------|--------------|--------|-----------|--------|
| `renewal_intent` | text (nullable) | `NULL` / `interested` / `thinking` / `not_interested` | Code（webhook 收到 QR 固定文字 / T3 關鍵字 fallback） | §5 T2/T3 觸發 | SYSTEM_PROMPT contextSuffix、admin/students 列表 |
| `renewal_intent_at` | timestamp with time zone (nullable) | `NULL` / ISO 8601 | Code（同上） | 寫 `renewal_intent` 的同時 | admin/students 顯示「最後意向時間」 |
| `renewal_intent_source` | text (nullable) | `NULL` / `qr_week10` / `qr_week11` / `qr_week12` / `free_text_keyword` / `manual_admin` | Code 或教練後台 | 寫 `renewal_intent` 的同時 | debug、觀察 QR 觸發有效性 |
| `renewal_confirmed_at` | timestamp with time zone (nullable) | `NULL` / ISO 8601 | 教練（admin/students 後台手動勾選） | 教練確認學員真的完成續報流程 | SYSTEM_PROMPT contextSuffix（永不提續報）、admin/students |

### Redis profile 鏡射欄位（一律 camelCase）

**防場景 1 事故復活的命名規則**：DB 是 snake_case、Redis profile 是 camelCase，`restoreUserFromSupabase` / `syncUserToSupabase` 在兩邊做轉換。

| DB 欄位（snake） | Redis profile（camel） |
|---|---|
| `renewal_intent` | `renewalIntent` |
| `renewal_intent_at` | `renewalIntentAt` |
| `renewal_intent_source` | `renewalIntentSource` |
| `renewal_confirmed_at` | `renewalConfirmedAt` |

**消費端一律讀 camelCase**（contextSuffix / notifyRenewalInterest / admin API）。

### QR 文字 → renewal_intent 映射表（唯一真相來源）

| QR 固定文字 | interest（現有 code） | renewal_intent（新） | notifyCoach | 觸發週數 |
|---|---|---|---|---|
| 想了解怎麼繼續 | yes | **interested** | true | w10, w11 |
| 我覺得可以自己來 | maybe → （契約重映射）| **not_interested** | true | w10, w11 |
| 還在想 | thinking | **thinking** | true | w10, w11 |
| 問助教續報方案 | yes | **interested** | true | w12 |
| 續報我再想想 | thinking | **thinking** | true | w12 |
| 我有其他問題想問 | null | **NULL（不寫入）** | false | w12 |

**設計決定**：
- 「我覺得可以自己來」現有 code 標 `interest: 'maybe'`（錯誤命名），新契約重映射為 `renewal_intent='not_interested'`。**`RENEWAL_QR_RESPONSES` 物件要新增 `renewal_intent` 欄位**，既有 `interest` 欄位保留（向後相容 notifyRenewalInterest）
- 「我有其他問題想問」**不寫入 renewal_intent**（學員只是轉話題）
- w10/w11 共用 3 選項（同 QR，不同推播話術）

### 既有欄位注意事項

| 欄位 | 注意 |
|---|---|
| `users.class_name` | 讀 classMap 拿 endDate、判定 classStatus |
| `users.week_number` | 備用參照 |
| `users.display_name` | 教練通知時顯示；若為 NULL → fallback `userId.substring(0, 8)` |
| `users.role` | **既有 write-through 不覆蓋 role**（syncUserToSupabase 白名單不含），新 4 欄沿用同設計（白名單必須明列） |

### ⚠️ syncUserToSupabase 白名單修正要求

`lib/user.js:258-266` 目前硬編碼 7 欄：`id / display_name / intro / intro_at / goal / week_number / updated_at`。**必須擴充為 11 欄**（加入 4 個新欄位）。不加 = write-through 寫不進去，silent fail。

---

## 3. AI 輸出契約

**本方案零 AI 判定**。renewal_intent 只由三個來源寫入：
- QR 固定文字（§2 對照表）
- 自由文字關鍵字 fallback（§5 T3，6 既有關鍵字）
- 教練後台手動（`intent_source='manual_admin'`，未來 Phase 3）

**未來 Phase C 若加入 AI 判定層**，擴充 enum 的 migration 順序必須安全：
```sql
-- 安全擴 enum 順序（drop 舊 constraint → 擴值 → NOT VALID 加新 constraint → VALIDATE）
ALTER TABLE users DROP CONSTRAINT renewal_intent_values;
ALTER TABLE users ADD CONSTRAINT renewal_intent_values
  CHECK (renewal_intent IS NULL OR renewal_intent IN ('interested','thinking','not_interested','<new_value>')) NOT VALID;
ALTER TABLE users VALIDATE CONSTRAINT renewal_intent_values;
```

Phase C AI 判定一律走 human gate（Dashboard 列 pending → 教練確認才寫），**不自動 push 教練**。

---

## 4. Placeholder / 變數規則

### `notifyRenewalInterest` 函式簽名（v1 新版）

**舊簽名**：`notifyRenewalInterest(studentName, interest)` — interest 用舊值 `yes/maybe/thinking/no`
**新簽名**：`notifyRenewalInterest(studentName, intent, evidence, oldIntent = null)`

- `intent`：新 renewal_intent 值（`interested/thinking/not_interested`）
- `evidence`：QR 固定文字或關鍵字命中原話
- `oldIntent`：降級情境傳入舊值，其他情境傳 `null`

**呼叫端改動**（route.js:529、route.js T3 段）：
- 舊：`notifyRenewalInterest(studentName, renewalReply.interest)`
- 新：`notifyRenewalInterest(studentName, renewalReply.renewal_intent, trimmed, oldIntent)`

### 唯一 INTENT_TO_LABEL 映射表（所有消費端共用此單一真相）

```js
const INTENT_TO_LABEL = {
  interested: '想繼續 ✅',
  thinking: '還在想 💭',
  not_interested: '自己試試看 💡（可再聊聊）',
};

const INTENT_RANK = { interested: 2, thinking: 1, not_interested: 0 };
```

**interestLabel 組裝邏輯**（降級和一般情境都走這段）：

```js
const isDowngrade = oldIntent && INTENT_RANK[intent] < INTENT_RANK[oldIntent];
const interestLabel = isDowngrade
  ? `⚠️ 意向變化：${INTENT_TO_LABEL[oldIntent]} → ${INTENT_TO_LABEL[intent]}`
  : INTENT_TO_LABEL[intent];
```

**原則**：舊 label 和新 label 都從同一張 `INTENT_TO_LABEL` 查，單一維護。舊 `interestMap`（yes/maybe/thinking/no）保留不用，新 code 一律走新 map。

### push 訊息 template

```
{降級時前綴「【意向變化提醒】」，其他情境無前綴}📋 續報意願
{studentName}：{interestLabel}
原話：{evidence}

→ 查看學員：https://coach-line-bot.vercel.app/admin/students
```

**降級時訊息開頭加「【意向變化提醒】」** — 提示教練這是轉折不是 spam，避免誤刪（2026-04-19 玉玲場景 1 事故延伸教訓）。

| 變數 | 來源 | NULL/fallback |
|------|------|---------------|
| `studentName` | `getUser(userId).info.name` → `user.display_name` | fallback 為 `userId.substring(0, 8)`（**不是「同學」**，便於教練 grep） |
| `interestLabel` | 3 值對照：`interested`→「想繼續 ✅」/ `thinking`→「還在想 💭」/ `not_interested`→「自己試試看 💡（可再聊聊）」**降級情境專用 label**：「⚠️ 意向變化：{舊 label} → {新 label}」 | 若 intent=NULL 不應觸發通知 |
| `evidence` | QR 點選時 = QR 固定文字；T3 關鍵字命中時 = 訊息原話（code point 安全截斷 50 字，見下方注意事項）；降級情境 = 學員本次點選的 QR 文字或關鍵字原話 | 任一情境必有值 |

### evidence 截斷實作注意（Feasibility 發現）

`.slice(0, 50)` 對 emoji/surrogate pair（例如 🎉、🫶）可能切到代理對半，學員 message 含 emoji 時會產生無效 UTF-16。

**正確作法**：
```js
const truncatedEvidence = [...rawText].slice(0, 50).join('');
```

`[...text]` 走 code point iterator，可正確處理中文 + emoji 邊界。

### SYSTEM_PROMPT contextSuffix 優先序（修正）

**第 1 層（最高優先，跨所有 classStatus）**：

- `renewal_confirmed_at != NULL` → 「這位學員已完成續報，永遠不要再提續報」
- **不受 `classStatus ∈ {graduating, grace}` gate 限制** — active、expired 都適用

**實作插入位置**：`route.js:1275` 的 `let contextSuffix = '';` 之後、L1276 `if (classStatus === 'graduating' || classStatus === 'grace')` 之前：

**必須用 if/else if 互斥結構**，避免第 1 層和 graduating block 的 first-time 分支疊加矛盾（第三輪 yi-challenge 洞 D：疊加會讓 AI 收到「別提續報」+「自然帶續報」的矛盾指令）：

```js
let contextSuffix = '';

// [新增] 第 1 層：confirmed_at 命中 → 獨佔，skip 後續所有 block
if (contextUser?.renewalConfirmedAt) {
  // 仍保留結業禁句（如果是結業期，話術衝突必須先禁）
  if (classStatus === 'graduating' || classStatus === 'grace') {
    contextSuffix += `\n\n【重要情境：這位學員的課程已結束，目前是結業寬限期】

⚠️ 絕對禁止的說法（很重要）：
- 不可以說「隨時都可以問我」「我一直都在」「什麼時候想聊都可以」
- 不可以暗示他可以像以前一樣無限使用小幫手
- 因為結業後小幫手的服務是有限的，說了會跟實際體驗衝突`;
  }
  contextSuffix += `\n\n【這位學員已完成續報】
不要再提續報的事，正常回答問題就好。可以自然地肯定他的決定（但不用每次都提），像朋友一樣聊天。`;
}
else if (classStatus === 'graduating' || classStatus === 'grace') {
  // [既有邏輯] 第 2-5 層：alreadyRenewed / alreadyMentioned / first-time
  // ... 既有 L1288-1319 整段搬進這個 else if
}
```

**為什麼必須 if/else if 不是兩個獨立 if**：現有 graduating block 的 first-time 分支（L1303-1309）會塞「在最後自然帶一段續報引導」。若第 1 層用獨立 if → contextSuffix 先加「不提續報」、後面 graduating block 的 first-time 又加「自然帶續報」→ **AI 收到互相矛盾的指令**。必須 if/else if 互斥，第 1 層命中時第 2 層完全跳過。

**實作注意**：contextUser 來源 = `getUser(userId)` 呼叫（L1264-1266 附近已執行），profile 含經 restoreUserFromSupabase 轉換後的 `renewalConfirmedAt`（camelCase）。

**第 2 層（僅在 classStatus ∈ {graduating, grace} 內生效）**：
- `renewal_intent === 'interested'` → 「這位學員表達過想繼續的意向，不要再提續報／推薦」

**第 3 層（既有 alreadyRenewed，保留當兜底）**：
- `chatHistory 6 關鍵字命中` → 「這位學員已經決定續報了，不再提」
- **兜底性質**：T3 寫入失敗 / 舊資料 / 跨對話 / Supabase 讀失敗時仍能生效

**第 4 層（既有 alreadyMentioned）**：
- Redis `coach-renewal-mentioned:{userId}` 當日內已設 → 「今天已提過，這次不要再提」

**第 5 層（default）**：
- first-time → 自然帶一次續報

---

## 5. 觸發規則表（State Transition Table）

### T1：QR 推播第 10 週五（新）

```
觸發：handleEveningPush cron + isFriday + courseWeek === 10 + renewal-w10 Redis key 不存在
條件：active 區間內（now >= startDate && now <= endDate，用 calcCourseWeek 判定，
      而非走 getClassStatus）
動作：
  - pushMessage(userId, week10Text)（見下方 getRenewalWeek10Message 定稿）
  - r.set(`coach-renewal-push:${userId}:w10`, now, ex: 86400*30)
  - recordPush(r, userId)
下一狀態：等 webhook 收到學員的 QR 點選 → T2
```

**getRenewalWeek10Message 定稿話術**（2026-04-20 一休定稿）：

```js
function getRenewalWeek10Message(name) {
  return {
    text: `${name}，兩週後就結業了，跟你聊一件事。

我觀察很久一件事——有些學員快到結業時會跟我說：「我好像沒學好」「效果好像沒想像中」，然後就默默離開了。

但老實說，你會這樣想，就代表你還在意。不在意的人早就鬆手了。

瘦下來是徒弟，維持才是師傅。真正的分水嶺不是第一期瘦了幾公斤，是接下來一個人怎麼走回原本的生活。我們的經驗是，只參加一期就結束的學員，復胖率還是蠻高的——不是沒學到，是習慣還沒長進身體裡，被拉回去的力量太大。

兩三期，是一個比較穩的錨點。

如果你心裡有一點點「還想再試一段」的感覺，那就再給自己一期的時間。不是為了再瘦多少，是讓這次辛苦建立的狀態，不要白走。

你現在是什麼感覺？`,
    qr: [
      { label: '想了解怎麼繼續', text: '想了解怎麼繼續' },
      { label: '我覺得可以自己來', text: '我覺得可以自己來' },
      { label: '還在想', text: '還在想' },
    ],
  };
}
```

**話術設計邏輯**（給下個 session 的 AI 理解為何這樣寫）：
- 打靶：7 成不續報的學員中，最多是「覺得自己沒學好／效果不如預期／跟不上」這類自我懷疑的人
- 翻轉敘事：「你會這樣想就代表你還在意」把自我懷疑重新框架為正向訊號
- 論據：「一期結束復胖率蠻高」「2-3 期是錨點」是一休過去真實經驗論據
- 續報框架：「再給自己一期的時間」把「續報」框成「給自己」不是「跟我們」
- 結尾開放問句 + QR 3 選項，不逼學員回答

### T2：QR 點選 → 寫 renewal_intent（加 gate）

```
觸發：webhook/route.js:517 getRenewalQRResponse(trimmed) 命中
條件（全部要成立）：
  - renewalReply.renewal_intent !== null（排除「我有其他問題想問」）
  - 是私訊（不是群組）— route.js:462 的 bufferAndSchedule 路徑本來就是私訊
  - classStatus ∈ { active 且 courseWeek >= 10, graduating, grace }
    → 避免課程第 3 週 copy-paste「問助教續報方案」污染 DB
動作（**必須按此順序執行，順序錯會讓降級判定失效**）：

1. **sendMessage**(replyToken, userId, renewalReply.reply)（既有）
2. **讀 oldIntent**：`const u = await getUser(userId); const oldIntent = u.renewalIntent || null;`
   → **重用 L526 既有 getUser 的回傳物件**（L526 已取 studentName 時呼叫過 getUser，
      直接從該物件讀 .renewalIntent，**不要**再次呼叫 getUser 浪費 round-trip）
   → **關鍵：oldIntent 必須在步驟 3 之前讀**，步驟 3 會覆蓋 DB + Redis profile，
      若順序倒置則 oldIntent 永遠等於新值，降級判定失效
3. **updateRenewalIntent**(userId, { intent: renewal_intent, at: now, source: `qr_week${week}` })
   → 原子化 Supabase UPDATE + Redis profile 同步（§10 helper 定義）
4. **算降級**：`const isDowngrade = oldIntent && INTENT_RANK[intent] < INTENT_RANK[oldIntent];`
5. **Cooldown 判定**：
   - 檢查 `r.get('coach-renewal-notify-cd:' + userId)`
   - **若 cooldown 未命中** → notifyRenewalInterest(studentName, intent, evidence=trimmed, oldIntent)
   - **若 cooldown 命中但 isDowngrade**（3 pair：interested→thinking / interested→not_interested / thinking→not_interested）
     → **豁免 cooldown，強制 push**（降級 = 教練必知道轉折）
     → interestLabel 自動用降級專用格式（§4 INTENT_TO_LABEL 組裝邏輯）
     → push template 開頭加「【意向變化提醒】」前綴
     → evidence 保留學員本次點選的 QR 原話（不是「意向更新」meta 文字）
   - **若 cooldown 命中且非降級**（相同值 / 升級）→ 不 push，只寫 DB
6. **設/重設** `coach-renewal-notify-cd:${userId}`, ex: 172800（無論 push 與否都 reset，保持 48hr 計時）

### 降級定義（豁免 cooldown 的三種情境）

用意向序號比對，**新值序號 < 舊值序號 = 降級**：

```
序號對照：interested = 2, thinking = 1, not_interested = 0
```

| 舊 intent | 新 intent | 是否降級 | 豁免 cooldown？ |
|---|---|---|---|
| interested (2) | thinking (1) | ✅ | ✅ 強制 push |
| interested (2) | not_interested (0) | ✅ | ✅ 強制 push |
| thinking (1) | not_interested (0) | ✅ | ✅ 強制 push |
| thinking (1) | interested (2) | ❌（升級） | 依 cooldown |
| not_interested (0) | interested (2) | ❌（升級） | 依 cooldown |
| not_interested (0) | thinking (1) | ❌（升級） | 依 cooldown |
| 相同值 overwrite | — | ❌ | 不 push |

實作：
```js
const INTENT_RANK = { interested: 2, thinking: 1, not_interested: 0 };
const isDowngrade = oldIntent && newIntent
  && INTENT_RANK[newIntent] < INTENT_RANK[oldIntent];
```
下一狀態：SYSTEM_PROMPT 下次觸發時讀 renewal_intent，走 §4 對應層
```

### T3：自由文字關鍵字 fallback（保留不動 + 加 gate）

```
觸發：webhook/route.js:1278 既有 renewalKeywords 命中
（6 關鍵字：'續報','繼續報','我有報','已經報了','報名了','我報了'）

條件：
  - classStatus ∈ { graduating, grace, expired }
    → expired 包含進來：「結業後學員突然說想續」是重要訊號
  - **renewalIntent IS NULL（從 profile 讀 camelCase）**
    → QR 已寫任何值（含 not_interested）都不覆蓋。確保 QR > 關鍵字

動作：
  - 維持既有 alreadyRenewed 邏輯（contextSuffix 不再提）
  - updateRenewalIntent(userId, { intent: 'interested', at: now, source: 'free_text_keyword' })
  - 48hr cooldown 判定：未命中 → notifyRenewalInterest（evidence = 截斷訊息 50 字）
  - 設 cooldown key

下一狀態：同 T2
```

### T4：教練後台勾選已續報

```
觸發：admin/students API POST /confirm-renewal
條件：admin key 驗證通過
動作：
  - UPDATE users SET renewal_confirmed_at=now() WHERE id=?
    → **一律用 plain UPDATE，禁用 jsonb_set**
    → 理由：2026-04-14 Awear personal_info 全滅事故教訓 — jsonb_set 帶子查詢回 NULL 會炸掉整個欄位。
      confirmed_at 是 timestamp 不是 jsonb，不會誤用 jsonb_set，但列入禁令防未來誤擴 pattern
  - Redis profile 同步 renewalConfirmedAt
  - 不碰 renewal_intent（保留原意向記錄）
下一狀態：SYSTEM_PROMPT §4 第 1 層永遠命中，不提續報
```

### T5：教練後台取消勾選（罕見）

```
觸發：admin/students API POST /unconfirm-renewal
條件：admin key 驗證通過
動作：
  - UPDATE users SET renewal_confirmed_at=NULL WHERE id=?
  - Redis profile 同步
下一狀態：回到 renewal_intent 驅動
```

### T6（新）：memberLeft 清 intent 欄位

```
觸發：webhook/route.js:83-98 memberLeft 事件（學員離開群組/被踢）
條件：既有 class_name 被清 NULL 時同步觸發（不論 class_name 原值是什麼）
動作（插入位置：route.js L91 update 之後、L93 log 之前）：
  1. UPDATE users SET
       renewal_intent=NULL,
       renewal_intent_at=NULL,
       renewal_intent_source=NULL,
       class_name=NULL
     WHERE id=?
     → **保留 renewal_confirmed_at**（表示上期有完成，業務紀錄不刪）
  2. Redis profile 同步（獨立函式，不走 saveUser 避免整包 sync 回 DB 覆蓋其他欄位）：
     - r.get(`coach-user:${userId}`) → delete profile.renewalIntent/.renewalIntentAt/.renewalIntentSource/.className
     - r.set 寫回
  3. 清除 Redis 輔助 keys：
     - del `coach-renewal-notify-cd:${userId}`
     - del `coach-renewal-push:${userId}:w10` / w11 / w12
     - del `coach-portrait:${userId}` / `coach-portrait-ver:${userId}`
       → Portrait cache 可能含「這位學員 interested」的人格描述，不清會讓重加入學員的 AI 觀察失真
下一狀態：等下次 assignment
```

### T7（新）：重加入班級清 intent 欄位（三個觸發源）

**觸發源 A：tryMatchPreloaded 自動換班（名單上傳時）**

```
位置：lib/user.js:450-466（現有已有 user.class_name !== s.className 比對邏輯，直接擴）
動作：
  1. 現有 Supabase update 語句擴充：
     .update({
       class_name: s.className,
       renewal_intent: null,
       renewal_intent_at: null,
       renewal_intent_source: null
       // renewal_confirmed_at 保留
     })
  2. 現有 Redis profile 同步段落（cached.class_name = ...）擴 4 camelCase 欄位清 NULL
  3. del Redis keys（同 T6 第 3 步）
注意：
  - batch import 中間某筆失敗 → 其他學員該清的要清乾淨
    （v1 接受 per-user 失敗獨立處理，不 rollback 整批；由 log 記錄失敗 userId 供教練手動修）
    → 列入 v2 TODO 「batch import 失敗 recovery」
```

**觸發源 B：admin/students API update class_name（教練手動改班）**

```
位置：app/api/admin/students/route.js:26-45
現狀：直接 .update(updates).eq('id', userId)，無 SELECT 舊值
要補的步驟：
  1. 若 updates.class_name 存在，先 SELECT 舊值：
     const { data: old } = await sb
       .from('users')
       .select('class_name')
       .eq('id', userId)
       .single();
  2. 若 old?.class_name !== updates.class_name（包含 null 情境 = 退費清空）：
     - 在 updates 加入：renewal_intent=null, renewal_intent_at=null, renewal_intent_source=null
     - 注意：confirmed_at **不加**（業務紀錄保留）
  3. 現有 .update(updates).eq('id', userId) 繼續跑
  4. 在 API handler 結尾呼叫 Redis 清理輔助函式（同 T6 第 3 步的 5 個 key）
```

**觸發源 C：memberJoined / followEvent 後的 assignment（如有）**

```
現狀：route.js:91 memberLeft 是清流程；join 流程目前由 tryMatchPreloaded 處理（觸發源 A）
T7 不額外擴。
```

**NULL 情境說明（清空 class_name = 退費）**：
- A / B 任一觸發源，`newClass = null` 且 `oldClass != null` → 仍視為「class_name 改變」，照 T7 動作清 intent 欄位
- renewal_confirmed_at 保留（若曾續報過表示業務紀錄）

下一狀態：新班重新走 T1→T2/T3 流程；退費學員 intent 為 NULL，不受推播影響

---

## 6. Handoff / Fallback 規則

### Handoff（轉人工）

- `renewal_intent = interested` → notifyRenewalInterest push → 教練主動聯絡
- `renewal_intent = thinking` → notifyRenewalInterest push → 教練可選擇是否聯絡
- `renewal_intent = not_interested` → notifyRenewalInterest push → 教練知情但 SOP 標「push 只通知、不要求行動」
- **降級**（interested → not_interested）→ 強制 push（豁免 cooldown），標「意向更新」讓教練看到轉折
- `renewal_confirmed_at` 由教練手動勾（業務流程完成後）

### Fallback

| 失敗情境 | Fallback |
|---------|----------|
| Supabase UPDATE 失敗 | log error，不阻塞對話回覆（write-through async 模式既有） |
| Redis cooldown 查詢失敗 | 視為未命中，允許 push（寧可多推不要漏） |
| getUser 拿不到 studentName | fallback `userId.substring(0, 8)`（**不是「同學」**） |
| QR 文字不在對照表 | 走原 QUICK_REPLIES / AI 流程（既有） |
| renewal_intent 欄位讀取失敗 | 視為 NULL，走 §4 第 3-5 層 |

### Cooldown 細則

- Key: `coach-renewal-notify-cd:{userId}`
- TTL: 48 小時（172800s）
- 範圍：跨 intent 值共用同一個 key
- **豁免條件**：
  - 降級（interested → not_interested）**強制 push**
  - `renewal_confirmed_at` 被 set 時（這是終態，必通知）
- 權衡：升級（thinking → interested）若 48hr 內已通知 → 不重推（教練已知，不加訊息量）

---

## 7. 多選 / 歧義處理

| 情境 | 處理 |
|------|------|
| **T2+T3 並發 race**（LINE webhook 不序列化） | 同一 userId 的 QR event + message event 可能並行進入；T3 若讀到 T2 寫入前的舊 profile（Redis 尚未 flush） → 兩者都可能 write。實務風險：最終 DB UPDATE 最後贏，但教練可能收 2 次 push。v1 接受此風險，見附錄 C #8（v2 加 optimistic locking） |
| 學員點 QR 後同句打關鍵字 | 兩則訊息分別觸發；T2 先命中（intent=值），T3 命中但 `renewalIntent IS NULL` 失敗 → 不覆蓋 |
| 先點「還在想」再點「問助教續報方案」 | T2 overwrite：thinking→interested（升級），cooldown 若命中不推 |
| 先點「問助教續報方案」再點「我覺得可以自己來」 | T2 overwrite：interested→not_interested（**降級**），**強制 push 豁免 cooldown** |
| 先 T3 命中「我報了」寫 interested，後點「我覺得可以自己來」 | T2 overwrite：interested→not_interested，觸發降級強制 push |
| 自由文字命中關鍵字但 QR 已寫過任何值（含 not_interested） | T3 條件 `renewalIntent IS NULL` 失敗，只 log 不寫、不 push |
| 教練已勾 confirmed_at 但學員又點 QR | intent 會被 overwrite，但 contextSuffix §4 第 1 層永遠優先讀 confirmed_at，行為不受影響 |
| 在群組打 QR 文字 | route.js:105 群組路徑不進 T2（bufferAndSchedule 是私訊獨有） |
| 課程第 3 週（active，但 courseWeek < 10）打 QR 文字 | T2 classStatus gate 擋下，不寫 intent、不 push |
| expired 學員說「我想續」 | T3 gate 允許 expired，寫 intent=interested + push |
| 重加入班級帶舊 intent | T7 清除 intent 欄位，新班從 NULL 重走 |

---

## 8. 閾值 / 常數表

| 常數 | 值 | 存哪裡 | 後台可改 |
|------|---|--------|---------|
| 通知 cooldown | 48 hr (172800s) | `webhook/route.js` 常數 | 否（v1） |
| QR 推播週數 | 10, 11, 12 | `smart-push/route.js` 條件 | 否（v1） |
| T2 classStatus gate | `active` 且 `courseWeek >= 10` / graduating / grace | 常數 | 否 |
| T3 classStatus gate | graduating / grace / **expired** | 常數 | 否 |
| evidence 截斷長度 | 50 字 | 常數 | 否 |
| renewal-push Redis TTL | 30 天（86400*30） | 既有 | 否 |
| renewal-notify-cd Redis TTL | 48 hr | 新增 | 否 |

---

## 9. 寫入規則（Data Lifecycle）

| 欄位 | 寫入模式 | 清除條件 | Freshness |
|------|---------|---------|-----------|
| `renewal_intent` | overwrite（QR 無條件；T3 僅 NULL 才寫） | T6 memberLeft、T7 重加入班級 | 無 TTL（持久） |
| `renewal_intent_at` | overwrite（與 intent 同步） | 同上 | 同上 |
| `renewal_intent_source` | overwrite（與 intent 同步） | 同上 | 同上 |
| `renewal_confirmed_at` | append-like（set 一次即有效，只教練能改） | 僅 T5 教練後台可 NULL；**memberLeft / 重加入班級不動** | 永久 |
| Redis `coach-renewal-notify-cd:{userId}` | set with TTL 172800s | TTL 自動過期；T6/T7 觸發時清除 | 48hr |
| Redis `coach-renewal-push:{userId}:w10`/`w11`/`w12` | set with TTL | TTL 30 天；T7 觸發時清除 | 30 天 |

### 關鍵原子性：`updateRenewalIntent` helper

**不能**走「getUser → mutate profile → saveUser」（會觸發 syncUserToSupabase 整包二次寫 + race condition）。

**必須**走「直接 Supabase UPDATE + 單獨 update Redis profile 欄位」：

```js
async function updateRenewalIntent(userId, { intent, at, source }) {
  const sb = getSupabase();
  // 1. DB 直寫
  await sb.from('users').update({
    renewal_intent: intent,
    renewal_intent_at: at.toISOString(),
    renewal_intent_source: source,
    updated_at: at.toISOString()
  }).eq('id', userId);

  // 2. Redis profile 欄位同步（只動 4 個 camelCase 欄位，不整包 saveUser）
  const r = getRedis();
  const profile = await r.get(`coach-user:${userId}`);
  if (profile) {
    profile.renewalIntent = intent;
    profile.renewalIntentAt = at.toISOString();
    profile.renewalIntentSource = source;
    await r.set(`coach-user:${userId}`, profile);
  } else {
    // Redis miss 時不強制 getUser（避免 recursion 風險），
    // DB 已寫入，下次 getUser read-through 會從 Supabase 重建 profile 帶入 4 camelCase 欄位
    // 注意：在 read-through 發生之前，SYSTEM_PROMPT 若被觸發會讀到 NULL intent，
    // 走 first-time 分支再問一次續報 — 這個 Redis miss window 是已知小洞，
    // 實務上：同一 session 內 getUser 通常已 cache 過 profile，新學員首次觸發 QR
    //         前一定經過 handleMessage → getUser（已建 profile）→ T2 觸發，profile 必存在
  }
}
```

**抄 pattern**：
- Supabase 直寫 + Redis 單欄位同步 → 抄 `lib/user.js:452-462` tryMatchPreloaded 的 class_name 更新寫法（**不是 setGoal**，setGoal 是獨立 key `coach-goal:` 不是 mutate profile）
- Redis miss 處理 → 依靠 `restoreUserFromSupabase` 的 read-through 機制

---

## 10. 責任分工

### Phase 0：前置確認 ✅ 全部完成

- [x] Supabase project `fnlkhxnfaylhqhystmbr` users 表 12 欄確認
- [x] Supabase KEY 是 secret key（service role 等級），bypass RLS，UPDATE users 無阻礙
- [x] Vercel STAFF_USER_ID 確認已設（Production / Preview / Development 三環境 31 天前已設）
- [x] 第 10 週推播話術定稿（§5 T1 `getRenewalWeek10Message`）
- [x] Phase 1 migration 完成（2026-04-20 執行 `add_renewal_fields`，4 欄 + 2 CHECK constraint 全建）

### Phase 1：Schema Migration（**走 MCP apply_migration**）

coach-line-bot 沒有 `supabase/migrations/` 目錄，既有 practice 是直接 `mcp__supabase__apply_migration`（不走 git 檔案）。

Migration name: `add_renewal_fields`

**整份 migration 是單一 transaction**（Supabase `mcp__supabase__apply_migration` 預設包 BEGIN/COMMIT，若中間任一 SQL 失敗全部 rollback，不會留下半套 schema）。未來若拆兩份跑必須手動包 transaction。

```sql
ALTER TABLE public.users
  ADD COLUMN IF NOT EXISTS renewal_intent text,
  ADD COLUMN IF NOT EXISTS renewal_intent_at timestamp with time zone,
  ADD COLUMN IF NOT EXISTS renewal_intent_source text,
  ADD COLUMN IF NOT EXISTS renewal_confirmed_at timestamp with time zone;

ALTER TABLE public.users
  ADD CONSTRAINT renewal_intent_values
    CHECK (renewal_intent IS NULL
           OR renewal_intent IN ('interested','thinking','not_interested'));

ALTER TABLE public.users
  ADD CONSTRAINT renewal_intent_source_values
    CHECK (renewal_intent_source IS NULL
           OR renewal_intent_source IN ('qr_week10','qr_week11','qr_week12','free_text_keyword','manual_admin'));
```

- 既有 row `renewal_intent` 全 NULL → CHECK 允許 NULL，不會被擋
- 兩個 CHECK constraint 分開命名，未來 v2 擴 enum 時可獨立 drop/re-add
- Feasibility 實測確認 users 表目前只有 `users_pkey` 一個 constraint，新增兩個 CHECK **零衝突**

### Phase 1.5：Redis/Supabase 同步層修正（**動 code 前必做**）

`lib/user.js` 改動：

**A. `syncUserToSupabase`（L253-271）** 白名單從 7 欄擴為 11 欄：

在現有 row 物件 literal（L258-266）加入（注意 `!== undefined` 防 profile 沒這 4 欄時 null 覆蓋 DB）：

```js
const row = {
  id: userId,
  display_name: profile.info?.name || null,
  intro: profile.introText || null,
  intro_at: profile.info?.intro_at || null,
  goal: profile.info?.goal || null,
  week_number: profile.weekNumber || null,
  updated_at: new Date().toISOString(),
  // [新增] 4 個 renewal 欄位，undefined 時不寫入（防覆蓋 DB）
  ...(profile.renewalIntent !== undefined && { renewal_intent: profile.renewalIntent }),
  ...(profile.renewalIntentAt !== undefined && { renewal_intent_at: profile.renewalIntentAt }),
  ...(profile.renewalIntentSource !== undefined && { renewal_intent_source: profile.renewalIntentSource }),
  ...(profile.renewalConfirmedAt !== undefined && { renewal_confirmed_at: profile.renewalConfirmedAt }),
};
```

**B. `restoreUserFromSupabase`（L174-229）** 加 snake→camel 轉換：

在 L208 附近的 profile 物件 literal 之後、L211 intro 解析前，加入：

```js
// [新增] 續報欄位 snake → camel 轉換
profile.renewalIntent = data.renewal_intent || null;
profile.renewalIntentAt = data.renewal_intent_at || null;
profile.renewalIntentSource = data.renewal_intent_source || null;
profile.renewalConfirmedAt = data.renewal_confirmed_at || null;
```

**C. 新增 helper `updateRenewalIntent`** — 抄 `lib/user.js:452-462` tryMatchPreloaded 的 Supabase update + Redis 單欄位同步 pattern（**不是抄 setGoal** — setGoal 用獨立 Redis key，不是 mutate user profile）。完整 code 見 §9 Data Lifecycle 區塊。

### Phase 2：主要 Code 實作（**必須按此子步驟順序獨立 deploy**）

**為什麼子步驟順序重要**：若先改 T2/T3（寫入端）但還沒改 contextSuffix（消費端）→ intent 寫入 DB 但 SYSTEM_PROMPT 還走舊邏輯 → 學員點完「問助教續報方案」後下次對話**仍被續報提示**（因為既有 alreadyRenewed 看 chatHistory 不看 renewal_intent）→ 學員困惑「我說過要續報了為什麼還問」（第三輪 yi-challenge 洞 G）。

**子步驟順序**：

**Phase 2.1（消費端先 ready）**：
- `contextSuffix` 重構（route.js:1274-1319）：改 5 層優先序，**第 1 層（confirmed_at）用 if/else if 結構避免跟 first-time 疊加**（§4 實作插入位置）
- build + deploy + 觀察：此時 intent 為 NULL（舊學員都沒寫過），第 1 層不會命中、第 2 層也不會命中，行為與 v0 完全相同

**Phase 2.2（INTENT_TO_LABEL 映射 + notifyRenewalInterest 新簽名）**：
- 新增 `INTENT_TO_LABEL` + `INTENT_RANK` 常數
- `notifyRenewalInterest`（L1687-1705）簽名改為 `(studentName, intent, evidence, oldIntent = null)`
  - 舊 `interestMap`（yes/maybe/thinking/no）**保留**，但新 code 不呼叫
  - 新增 `INTENT_TO_LABEL` 查表 + 降級 label 組裝邏輯
  - push template 加「【意向變化提醒】」降級前綴
- build + deploy + 觀察：此時呼叫端還走舊簽名，函式同時支援新舊呼叫

**Phase 2.3（QR 映射表擴欄位）**：
- `RENEWAL_QR_RESPONSES`（L1645-1678）每筆加 `renewal_intent` 欄位（§2 對照表）
- `getRenewalQRResponse` 回傳物件自然包含新欄位（讀寫分離，既有呼叫不受影響）
- build + deploy + 觀察：此時仍未寫 DB，純擴結構

**Phase 2.4（寫入端：T2 + T3 + 生命週期清除）**：
- T2 路徑（L516-533）：加 classStatus gate + 動作 6 步標號（§5 T2）+ 呼叫 `updateRenewalIntent` + 呼叫新簽名 `notifyRenewalInterest`
- T3 路徑（L1278）：加 classStatus gate（含 expired）+ `renewalIntent IS NULL` 條件 + 呼叫 `updateRenewalIntent`
- T6 memberLeft handler（L83-98）：擴 Supabase update + Redis 清除 + Portrait cache 清除
- T7 tryMatchPreloaded 自動換班（lib/user.js:450-466）：擴現有 update
- T7 admin/students API（L26-45）：新增 SELECT 舊值 + 比對後加 renewal 清欄位
- build + deploy + 觀察：**學員第一次走 T2 寫入，SYSTEM_PROMPT 第 1 層會讀到新值**

**Phase 2.5（cooldown 去重 + evidence 參數）**：
- 新增 Redis key `coach-renewal-notify-cd:{userId}` TTL 172800s
- 降級豁免邏輯（§5 T2 步驟 5）
- evidence 走 `[...text].slice(0, 50).join('')` 防 emoji 代理對

`smart-push/route.js`：
- `handleEveningPush` 新增 w10 + isFriday 分支（插在 L528 之前）
- 共用 w11 QR 3 選項，推播話術第 10 週版另寫（待一休確認）

memberLeft / 重加入班級清欄位：
- `webhook/route.js:91` memberLeft handler → T6
- `lib/user.js:450` tryMatchPreloaded 自動換班 → T7
- `admin/students` API update class_name → T7（API 層檢測 newClass !== oldClass）

### Phase 3：後台 UI（可獨立於 Phase 2 後做）

- `admin/students/page.js`：列表顯示 renewal_intent / intent_at / confirmed_at 欄位
- 新增「確認已續報」按鈕 → POST `/api/admin/students/confirm-renewal`
- 新增「取消確認」按鈕 → POST `/api/admin/students/unconfirm-renewal`
- 現有白名單 update API（`admin/students/route.js:26-40`）擴充以支援 renewal_confirmed_at

### Phase 4：推播 w10 上線

- cron 不變（每天 20:10 evening）
- w10 分支插入後第一次觸發的 w10 五 → 驗證 log
- 走 dry-run 測試：用 `?dry=1` 查 log 看推播名單對不對

### 依賴關係（嚴格順序）

```
Phase 0 確認 → Phase 1 migration → Phase 1.5 sync 層 → Phase 2 code → Phase 3 UI → Phase 4 上線
```

- Phase 1 沒做 = Phase 2 寫 DB silent fail（欄位不存在）
- Phase 1.5 沒做 = Phase 2 寫 DB 但 Redis profile 沒同步 = SYSTEM_PROMPT 讀不到
- Phase 2 沒做 = Phase 4 推播後學員點 QR 不會記 intent
- Phase 3 不做 = renewal_confirmed_at 永遠 NULL（但不影響 intent 流）

---

## 11. 跨系統介面

### 對阿算 Bot（abc-line-bot）
- **無影響**。coach-line-bot 有獨立 Supabase project（`fnlkhxnfaylhqhystmbr`），users 表 coach 獨有
- Grep 確認 abc-line-bot 不讀 coach 的 users 表
- CLAUDE.md「兩個 Bot 共用 Supabase」對 coach 已不成立 — 是文件 drift（另工單修正）

### 對教練後台（admin/students）
- 新增欄位讀取責任：顯示 renewal_intent / intent_at / confirmed_at
- 新增寫入 API：confirm-renewal / unconfirm-renewal
- 契約：schema 不變、只 append；下游要讀必須兼容 NULL

### 對推播 cron
- 新增 w10 推播分支（evening 內）
- w11/w12 既有不動
- renewal-log Redis key prefix 不變

### 對 Dashboard（學員個人頁）
- **v1 不顯示**給學員（避免學員知道自己被標 `not_interested` 造成心理負擔）
- v2 可能顯示給已 confirmed_at 的學員（正面語意）

### 對既有 write-through 行為
- `syncUserToSupabase` 既有行為：**只帶白名單欄位，其他欄位（如 role / class_name / journey / last_group_activity）不在 row → upsert 不覆蓋**
- 新 4 欄納入白名單後，write-through 會正確同步
- 既有 `role` 欄位行為不受影響（admin API 直接 update，不經 syncUserToSupabase）

---

## 12. 驗收標準（Acceptance Criteria）

**所有測試必須用 testing userId（建議用固定測試帳號 `TEST_COACH_USER_ID`），不得在真學員 ID 上跑。**

**Phase 0 完成**：
- [ ] 確認 Vercel 生產環境 `STAFF_USER_ID` 已設
- [ ] 第 10 週推播話術定稿

**Phase 1 完成**：
- [ ] Supabase users 表查 information_schema，新 4 欄存在且型別正確
- [ ] 兩個 CHECK constraint 生效（寫入非枚舉值 → 422 error）
- [ ] 既有 row renewal_intent 預設 NULL 通過驗證

**Phase 1.5 完成**：
- [ ] `updateRenewalIntent` helper 單元驗證：寫 Supabase + Redis 都有，兩邊一致
- [ ] `syncUserToSupabase` 寫入新 4 欄可寫入
- [ ] `restoreUserFromSupabase` 讀回 profile 的 camelCase 欄位有值

**Phase 2 完成**：
- [ ] 測試帳號點「問助教續報方案」→ Supabase `renewal_intent='interested'`，`source='qr_week12'`
- [ ] 48hr 內第二次點同 QR 不重複 push 教練（cooldown 生效）
- [ ] 測試帳號先點「問助教續報方案」再點「我覺得可以自己來」→ Supabase 變 `not_interested`，**教練收到第二次 push（豁免 cooldown）標記降級**
- [ ] 測試帳號在 active w3 打「問助教續報方案」→ **T2 gate 擋下，Supabase 無寫入、教練無通知**
- [ ] 測試帳號在 expired 打「我報名了」→ T3 命中，Supabase `intent='interested'`，`source='free_text_keyword'`
- [ ] 測試帳號有 QR 寫過 `not_interested` 後自由文字打「我報了」→ T3 條件擋下，Supabase 不覆蓋
- [ ] 教練後台勾選已續報 → SYSTEM_PROMPT curl test 回覆含「不再提續報」
- [ ] 測試帳號 memberLeft → Supabase 4 個 intent 欄位清空，confirmed_at 保留
- [ ] 測試帳號自動換班 → 同上

**Phase 3 完成**：
- [ ] admin/students 顯示每位學員 renewal_intent / intent_at / confirmed_at
- [ ] confirm-renewal / unconfirm-renewal API 能切換 confirmed_at

**Phase 4 完成**：
- [ ] 第 10 週五 20:10 cron dry-run log 正確列出該推播的 userId

---

## 附錄 A：現有 code 鎖定位置（給下個 session 的 AI）

| 動作 | 檔案 | 行號（2026-04-20 快照） |
|------|------|-----------|
| 讀 QR 固定文字對照 | `coach-line-bot/app/api/webhook/route.js` | 1645-1678 |
| QR 路由入口 | `coach-line-bot/app/api/webhook/route.js` | 516-533 |
| notifyRenewalInterest | `coach-line-bot/app/api/webhook/route.js` | 1687-1705 |
| 自由文字關鍵字掃描 | `coach-line-bot/app/api/webhook/route.js` | 1278-1281 |
| contextSuffix 組裝 | `coach-line-bot/app/api/webhook/route.js` | 1274-1319 |
| memberLeft handler | `coach-line-bot/app/api/webhook/route.js` | 91 附近 |
| w11 推播 | `coach-line-bot/app/api/cron/smart-push/route.js` | 528-549 |
| w12 推播 | `coach-line-bot/app/api/cron/smart-push/route.js` | 816-855 |
| w11 QR template | `coach-line-bot/app/api/cron/smart-push/route.js` | 278-287 |
| w12 QR template | `coach-line-bot/app/api/cron/smart-push/route.js` | 289-298 |
| handleEveningPush 主 loop | `coach-line-bot/app/api/cron/smart-push/route.js` | 465-666 |
| Supabase client | `coach-line-bot/lib/supabase.js` | 9-15 |
| saveUser / syncUserToSupabase 模式 | `coach-line-bot/lib/user.js` | 234-271 |
| setGoal 模式（updateRenewalIntent 抄這個） | `coach-line-bot/lib/user.js` | 994-1030 |
| tryMatchPreloaded 自動換班 | `coach-line-bot/lib/user.js` | 450-735 |
| admin/students update API | `coach-line-bot/app/api/admin/students/route.js` | 26-45 |
| admin/students UI | `coach-line-bot/app/admin/students/page.js` | 全檔 |
| Supabase project ID | — | `fnlkhxnfaylhqhystmbr` |
| Supabase KEY | `.env.local` SUPABASE_KEY | `sb_secret_*`（service role 等級，bypass RLS） |

---

## 附錄 B：尚未解決的問題

1. ~~**env var `STAFF_USER_ID`**~~ — ✅ 2026-04-20 完成確認：Vercel Production / Preview / Development 三環境 31 天前已全設（`vercel env ls production`）
2. ~~**第 10 週 QR 推播話術**~~ — ✅ 2026-04-20 一休定稿：打 7 成不續報學員的自我懷疑，翻轉敘事「你會這樣想代表你還在意」+ 把續報框架成「再給自己一期的時間」。完整話術見 §5 T1 `getRenewalWeek10Message`
3. ~~「我覺得可以自己來」是否通知教練~~ — 保留通知，interestLabel 改「自己試試看 💡（可再聊聊）」中性化
4. **admin/students 後台 UI 必做性** — 若不做 Phase 3，renewal_confirmed_at 永遠 NULL，v1 只靠 renewal_intent 一條線作用。建議 Phase 2 後立即做

---

## 附錄 C：v2 TODO（延後處理）

1. `already_renewed` enum 值（Phase C AI 判定時加）— 目前 T3 命中「我報了」仍寫 `interested`，教練收到 evidence 原話能自己判斷
2. evidence cooldown 跨來源改「同 intent 值才 dedupe」（v1 保守處理）
3. `intent_history` append-only log 表（若未來想 audit 意向變遷；journey 停用教訓啟發：保留 append-only 比累積式更安全）
4. 學員 Dashboard 顯示 renewal_confirmed_at 狀態（正面用途）
5. Phase C AI 判定層 + human gate pending 機制
6. **batch import 失敗 recovery**（T7 觸發源 A）— v1 接受 per-user 失敗獨立處理、不 rollback 整批，由 log 記錄失敗 userId 供教練手動修。v2 可加 retry queue 或 transaction-per-batch 機制
7. **同 48hr 多次 QR 點擊教練資訊完整度**（§7 新 7 教訓）— 中間 intent 變動已寫 DB 但不 push，教練要從 admin/students 後台看完整意向歷史才能判斷；v2 可加「24hr 摘要 push」把當日意向變化打包一次給教練
8. **race condition 雙寫防護**（§5 T2 oldIntent 讀取 → update 之間理論有 race）— LINE webhook 通常 single-user 序列化、實務風險極低；v2 若碰到可加 Redis lock 或 UPDATE...WHERE renewal_intent IS [oldValue] 做 optimistic locking

---

## 附錄 D：Phase 2 實作注意事項彙整（實作時必讀）

本節彙整 Feasibility / Adversarial 發現、散落在 §2/§5/§9/§10 的實作指引：

1. **命名規則（防場景 1 事故復活）**：DB snake_case / Redis profile camelCase，restore/sync 層做雙向轉換。消費端（contextSuffix、notifyRenewalInterest、admin API）一律讀 camelCase
2. **updateRenewalIntent 抄 pattern**：`lib/user.js:452-462` tryMatchPreloaded 的 class_name 寫法，**不是** setGoal（setGoal 是獨立 Redis key，不是 mutate user profile）
3. **syncUserToSupabase row object 擴充**：用 `... && { key: val }` spread pattern 防 undefined 覆蓋 DB，見 §10 Phase 1.5.A
4. **contextSuffix 第 1 層**：插在 `route.js:1275` 既有 `let contextSuffix = '';` 之後、L1276 classStatus if block 之前
5. **T2 oldIntent 讀取**：重用 `route.js:L526` 既有 getUser 回傳物件，不要二次 round-trip
6. **T7 admin API SELECT**：admin/students/route.js 現狀無 SELECT 舊值，必須補 5 行 code（見 §5 T7 觸發源 B）
7. **evidence 截斷**：用 `[...text].slice(0, 50).join('')` 走 code point 邊界，**禁用** `.slice(0, 50)` 切到 emoji 代理對半
8. **confirm-renewal API**：plain UPDATE 寫 timestamp，**禁用 jsonb_set**（歷史重演防呆）
9. **Portrait cache 清除**：T6/T7 觸發時一併 del `coach-portrait:{userId}` + `coach-portrait-ver:{userId}`，避免人格觀察快取污染
10. **Redis miss window 已知**：updateRenewalIntent 碰到 Redis profile 不存在時 silent skip，依靠下次 getUser read-through 回補。實務上 T2 觸發前必經 handleMessage → getUser，profile 必存在。首次加好友學員走 follow 流程建 profile，不會在 T2 前出現 miss
11. **測試隔離**：§12 驗收標準所有測試用 `TEST_COACH_USER_ID` sandbox，禁止在真學員 ID 上跑 overwrite 類測試

---

## 附錄 D：CLAUDE.md / 指揮中心更新待辦（Phase 4 完成後）

1. coach-line-bot/CLAUDE.md：
   - 更新 Supabase users 表欄位清單（12→16 欄）
   - 更新 Redis 資料結構（加 `coach-renewal-notify-cd:{userId}`）
   - 修正「兩個 Bot 共用 Supabase」文字為「獨立 project」
   - 修正「anon key」為「secret key（service role 等級）」

2. coach-line-bot/指揮中心.md：
   - 異動紀錄加 2026-04-21 續報記錄契約 v1 上線
   - 資料流向表加 renewal_intent / renewal_confirmed_at 欄位

3. AI_asis/CLAUDE.md 關鍵檔案導航：
   - 加 `coach-line-bot/契約_續報記錄.md`

---

*本契約 v1-review，等第二輪 yi-challenge 無新洞後升版為 v1。*
