# 休校長小幫手指揮中心

> 小幫手的架構全貌 + 資料流 + 即時狀態。
> **開工：讀這份 + CLAUDE.md。做完：更新「最近異動」。**
> 最後更新：2026-04-30（§3.5 #2/#3 + §2 P3 規則落地）

---

## 這份文件怎麼用

**指揮中心 = 架構 + 流程 + 索引 + 即時狀態。**
讓每個 session 知道系統怎麼運作、改什麼會影響什麼、有疑問去哪找。

### 記錄什麼

| 區塊 | 內容 | 為什麼在這裡 |
|------|------|-------------|
| 即時狀態 | 最近異動、待同步、已知問題 | 只有這裡追蹤動態狀態 |
| 架構全貌 | 每個部門的設計目的 + 核心流程 + 檔案 | 讓 session 知道全局怎麼運作 |
| 核心資料流 | 完整流程圖（每步標檔案路徑） | 讓 session 知道資料怎麼流、去哪找細節 |
| 資料相依 | 誰讀誰寫 | 改一個表時知道影響誰 |
| 索引 | 任務→去哪找、檔案→設計邏輯在哪 | session 有疑問時直接查 |
| 影響範圍 | 改什麼→影響什麼 | 動手前評估風險 |

### 不記錄什麼（這些在別的地方）

| 不放這裡 | 去哪找 |
|----------|--------|
| Redis keys 完整列表 | CLAUDE.md「Redis 資料結構」 |
| Supabase schema 完整定義 | CLAUDE.md「Supabase 資料表」 |
| Credentials / 環境變數 | CLAUDE.md |
| 完整檔案清單 | CLAUDE.md「檔案結構」 |
| Code 邏輯 | 讀對應的 .js 檔案 |

### 更新規則

- **最近異動**：保留 10 筆，超過刪最舊的
- **待同步**：完成的直接刪掉，不留 ✅ 記號
- **已知問題**：修完的刪掉
- **新流程**：寫完整流程圖，每個步驟標檔案路徑
- **新模組**：歸到對應部門，3-5 行（設計目的 + 流程 + 檔案）
- **不要複製 CLAUDE.md 的內容過來**，用「詳見 CLAUDE.md XXX 段」指過去

---

## 即時狀態（每次開工先讀）

> **sync 指揮中心**：一休說這句時 →
> 1. 「最近異動」加一筆  2. 「待同步」更新  3. 「資料相依」如果改了讀寫關係就更新

### 模組狀態

| 模組 | 專案位置 | 部署 | 狀態 |
|------|---------|------|------|
| Bot 核心 | `coach-line-bot/` | coach-line-bot.vercel.app | ✅ |
| 管理後台 | `coach-line-bot/app/admin/` | coach-line-bot.vercel.app/admin | ✅ |
| 助教後台 | `coach-line-bot/app/staff/` | coach-line-bot.vercel.app/staff | ✅ |
| 學員 Dashboard | `coach-line-bot/app/dashboard/` | coach-line-bot.vercel.app/dashboard | ✅ |
| 學員頁面 | `coach-line-bot/app/` (guide/quiz/knowledge/check) | coach-line-bot.vercel.app/* | ✅ |
| 群組偵測 | `lib/ai.js` + `lib/pending.js` | — | ✅ 精準模式（04-08 重啟） |

### 最近異動（保留最近 10 筆）

| 日期 | 模組 | 異動內容 | 影響 |
|------|------|---------|------|
| 07-06（下午2） | 群組偵測補漏 | **心得/自介有時被漏的三個洞**：① basicMessageFilter 8字門檻擋掉短心得（「瘦了3公斤！」6字到不了AI）→ 降5字、上限2000→4000；② 工作人員 displayName includes 比對，'jie'/'chao'/'凜' 誤傷學員名 → 英文 word-boundary / 單字中文全等（實測 Angelie/Jessie/凜月 不再誤殺）；③ AI「純打卡不通知」太嚴 → 帶進步數字/情緒的打卡算 isAchievement。觀察項：通知量可能增加，太吵再調回 | ai.js, route.js (9317eee) |
| 07-06（下午） | 群組草稿優化 | **一休後台複製貼上流程優化**：(a) @ 改 code 保證 — 兩個 draft 函式加 displayName 參數，`finalizeGroupDraft` 雙段剝 AI 自加的 @（先剝 @完整顯示名防「@Cindy Wu Wu」殘留）後統一冠 `@LINE顯示名`，漏 @ / @真名 從此不可能；admin/draft 手動路徑用教練輸入的名字。(b) 上下文 8→20 則全量 + 每則相對時間標記 + 觸發訊息標名字 + `slice(0,-1)` 去尾防觸發訊息重複（對齊 aiDetectQuestion）。(c) GROUP_DRAFT_PROMPT 加三段：稱呼規則（LINE 名不用真名）/ 先讀上下文（同學員連發合併理解、時間差判斷、不足不腦補）/ 情緒型 vs 資訊型（接一句就轉行動，防罐頭同理）+ 只答他問的。ACHIEVEMENT 同款。user-eye 雙視角審查 2🔴2⚠️ 全修，finalize 7 邊界 case 實測過 | ai.js, route.js, admin/draft |
| 07-06 | 🔴 記憶更正根因修復 + 續報 T3 + 名單比對 | **蘿蔔乾/超商循環根治**（Renee 澄清 8 次無效、Cindy 4 次）：根因 = 糾正只標 Supabase goals replaced，Redis `coach-goal:` 無 TTL 且 getActiveGoal Redis 優先 → 錯誤目標永生（production 實錘：兩人 Redis goal 存活 3 個月，已手動 del 止血）。修：(a) `cleanCorrectedMemory` 共用 helper（route.js 即時 + reconcile 兜底都掛，補清 coach-goal + goals 比對擴到 context 欄位）；(b) 新增 `coach-facts:{uid}` 永久事實 — 糾正 AI 同 call 產出正向事實句（只記客觀事實不記評價/病史），buildUserContext 第 5 參數永遠注入，防台灣中心知識建議長回來，糾正-糾正機制可回收，reset-user 會清；(c) **契約 T3 補實作**（定版但漏做，上線 2.5 個月 renewal_intent 全表 0 筆，07-02 一天 6+ 學員打字說已續報全沒記錄）— 自由文字 6 關鍵字 + graduating/grace/expired + intent NULL gate → 寫 interested + 48hr cooldown 通知教練；(d) batchImportIntros 比對改 normalizeName 全表 map（解 04-22 遺留的 8/140 miss，移除 intro.ilike 誤配，DB 重名 ambiguous 跳過），換班仍走 performClassChange。審查：yi-challenge 雙 agent + route-reviewer 🟢 + user-eye 修正已套用 | user.js, route.js, smart-push, reset-user |
| 04-30（下午） | ✅ §3.5 #2/#3 + §2 P3 prompt 5 條規則落地（4 commit） | **接交接指令把前 4 項待辦做完**：(a) `51c6fe4` §3 讀取路徑對齊 — staff/students + smart-push 改用 lib/classes.js getClass / getActiveClassNames，端到端測 /staff 回 123 人 + classMap 含 4/3 月班 + currentWeek 正確；(b) `df9e567` §3.5 #2 coach_quiz_collected read-through — webhook L924-948 inline，length=0 fallback Supabase + sadd 回寫 Redis SET；(c) `ccd2641` §3.5 #3 pending_responses 表落地 — 13 欄位 + redis_id UNIQUE + status soft delete + lib/pending.js 全面整合 + 新 backfill API，backfill 32 筆全成功；(d) `8deff24` §2.2 P3 規則 2/3/4/6/8 補上 — ACHIEVEMENT_DRAFT_PROMPT 加【你是誰】+【食物觀】+【ABC 用詞】3 區塊。Health 三服務綠燈。**剩 #5 normalize 抽函式（風險高） + #6 streak 持久化留下次 session** | webhook.js / classes 對齊 / pending.js + backfill-pending 新檔 / ai.js prompt + Supabase pending_responses 表 |
| 04-30 | ✅ §3 classes 表落地 + backfill 完成 | **契約 §3 資料持久化第一輪施工完成**：Supabase 新建 `classes` 表（9 欄位 + 2 partial index + soft delete `deleted_at`）→ `lib/classes.js` 新檔（getClass / saveClass / deleteClass / getClassStatus / getActiveClassNames / listAllClasses / backfillClassesToSupabase，含 Read-through + Write-through + camelCase ↔ snake_case normalize）→ `lib/user.js` 改 re-export → `staff/classes` API 改用新 lib（POST 雙寫，DELETE soft delete）→ 新 `admin/backfill-classes` API。一休 2026-04-30 決策：soft delete（資料永久保存）+ 不開 RLS（與其他表一致）+ 一個合併 commit。Backfill 跑完寫入 3 班（4/3/1 月班）。**讀取路徑（staff/students + smart-push）對齊留下次** | lib/classes.js 新檔, user.js, staff/classes, admin/backfill-classes 新檔, classes 表新建 (commit c472c9f) |
| 04-29 | ✅ §1 Bot 人格契約落地 | **解 SYSTEM_PROMPT 自我矛盾**（line 28 鼓勵「我以前也是」vs line 123 禁止）：4 處改動 + GROUP_DRAFT_PROMPT 1 處。L27/L92 身體經驗話術改第三人稱（「休校長以前也走過這條路」「很多同學都遇過這個」）+ L121-127「不要自稱 AI」改為「不主動自稱 AI（被問時坦誠回答）」+ 新增【被問「你是 AI 嗎？」的回答方式】段（從 GROUP_DRAFT 遷移）+ L590 GROUP_DRAFT 開頭範例同樣修。實測三場景全過 | ai.js (commit ea2c29e) |
| 04-29 | 📋 系統規則契約 v1 定版 | **基於 Shair 事件後做的 yi-frame 全面審視 + yi-contract 產出**：5 個子契約（Bot 人格 / Prompt 規則矩陣 12×4 / 資料持久化 A/B/C 分類 / Redis 命名 / Cron 觸發）整合一份 `契約_系統規則.md`（~500 行）。發現 SYSTEM_PROMPT 自我矛盾 + 5 個 Redis key 缺 Supabase 持久化 + ACHIEVEMENT_DRAFT_PROMPT 漏 5 條規則。索引在文件導航區 | `契約_系統規則.md` 新檔 |
| 04-29 | 🔴 圖片幻覺修補 | **Shair 事件根因 + 三層防線**：私訊收到圖片+「這是圖片」hint 文字時 AI 腦補照片內容（純碳水/份量/菜色）。三層防線：(L1) route.js image 進來 set Redis flag `coach-image-pending:{userId}` TTL 60s → 文字 buffer 進入 processBatchedMessages 讀 flag 注 marker prefix 進 handleMessage；(L2) PHOTO_KEYWORDS 擴增 7 句式（「這是圖片/這張照片/這張圖/看一下這/幫我看這/剛剛傳的」）+ AI judge 不誤殺；(L3) SYSTEM_PROMPT 補【你的能力限制】段（之前只在 GROUP_DRAFT_PROMPT 有，私訊雙標）。實測三場景全過。**核心教訓：prompt 防線在多 entry point 系統會雙標 → 必須 code+keyword+prompt 三層** | route.js, ai.js (commit a25e9fd) |
| 04-22 | 🔴 續報換班 bug **根治完成** | **Redis profile class key 統一 camelCase**（tryMatchPreloaded / auto-reassign / admin API / restoreUserFromSupabase 四路徑對齊）+ batchImportIntros 回寫 preload.matched + 清 renewal 4 欄 + 清 portrait/cooldown/push cache + 新增 backfill-class-sync API + admin API T7 觸發源 B 補 Redis 同步 + **backfill 跑完修 19 profile + 21 preload.matched + 手動救 8 人 class_name 空值** | user.js, admin/students/route.js, backfill-class-sync 新檔 |
| 04-21 | 防流失推播 | **沉默 7+ 天推播改問題診斷型** + Quick Reply 完整實作（學員可直接點選回覆原因） | smart-push/route.js |
| 04-21 | 後台 | admin 頁面收到 401 自動清 localStorage 並踢回登入 | admin/page.js |
| 04-20 | 續報記錄 | **契約 v1 定版 + Phase 1-3 全部完成**（三輪 yi-challenge 通過）— QR → renewal_intent → admin 確認 → w10 推播 → T7 換班清 4 欄 | route.js, user.js, smart-push, admin/students, Supabase schema |
| 04-12 | 糾正偵測 | code gate + AI 判定 + code 清三層（tags/goals/Redis）。即時偵測 + evening cron 兜底。Renee 蘿蔔乾事件 | route.js, smart-push |
| 04-12 | 結業控制 | graduating/grace 無限回覆但禁說「隨時都可以」+ expired 一天一次 + 續報引導一天一次 + 已續報偵測 | route.js |
| 04-08 | 群組偵測 | 重啟群組偵測 — 修非文字訊息錯頻 bug + 草稿加群組上下文 + 工作人員排除 | route.js, ai.js |
| 04-08 | 草稿品質 | GROUP_DRAFT_PROMPT 強化 — 跳餐/成分表引導 + 不編造規則 | ai.js |
| 04-08 | 推播 | 沉默 7+ 天改推知識題目 + 3 天冷卻 + 多句隨機 | smart-push/route.js |
| 04-08 | 後台 | PR#1 — staff 分班 rowKey + preloaded 幽靈記錄 | staff/page.js, user.js |
| 04-04 | 成本 | Gemini Context Caching — SYSTEM_PROMPT 共用 cache 省 67% | ai.js, gemini-cache.js |

### 待同步

- [ ] 群組偵測觀察期：通知訊噪比、confidence 門檻是否要從 0.8 降到 0.7
- [ ] 食物測驗歷史資料回填（部分舊用戶只有 Redis 沒有 Supabase）
- [ ] **v2 admin/students API 偵測 rows=0 回 404** — 這次 bash UID readonly 踩雷時 API 回 ok:true 但 0 rows affected，沒發現錯誤
- [ ] **v2 T6 memberLeft 清 renewal 欄位實作**（契約 §5 T6 code 未落地）
- [ ] **v2 expired 硬 reply 前加「已續報偵測」豁免**（洞 2 防守層：即使資料層漏，學員自己說「我續報了」能解鎖對話。07-06 T3 已寫 intent，此項剩「解鎖當日回覆上限」部分）
- [ ] **coach-facts 補 Supabase 雙層備份** — 07-06 永久事實 v1 只存 Redis（該 session 無 DDL 權限），MCP 授權後加 users 表欄位 + 同步層
- [ ] **QR 續報推播零點擊調查** — renewal_intent 上線 2.5 個月 QR 路徑（T2）也是 0 筆寫入，conversations 全表查無人打過 QR 固定文字。w10/w11/w12 推播有發（07-02 有推播訊息）但學員都用打字回，QR 選項點擊率 0%。考慮檢視 QR 設計或依賴 T3

### 已知問題

- 🟡 群組 @回覆仍可能有邊界 case（已加防護但未大量驗證）
- 🟡 舊用戶 journey 舊版語氣（等下次觸發重寫）
- 🟡 46 個 preload noMatch 中有 38 個可能真沒加好友（舊班舊生或 3 月班新生漏了）— 等他們加友會走 tryMatchPreloaded 正確寫 camelCase，不影響既有對話

---

## 架構全貌

```
休校長小幫手
├── I.    訊息入口 ─── LINE webhook → 簽名驗證 → buffer 合併 → 分流
├── II.   AI 分析 ──── SYSTEM_PROMPT + 三層模型 + 意圖分類
├── III.  知識系統 ─── AI 意圖分類 + 兩層式知識注入（Tier1 + Tier2 x21）
├── IV.   記憶系統 ─── Redis（快取）+ Supabase（永久）+ Read-through 回補
├── V.    用戶智能 ─── 標籤提取 + 即時 tags 注入（journey/summary 已停用）
├── VI.   群組偵測 ─── 精準模式（≥0.8 才通知 + 30min 冷卻）+ 班級結業控制
├── VII.  推播與遊戲化 ── 智慧推播 + 食物測驗 + 知識挑戰 + 健康存摺 + 目標
├── VIII. 管理後台 ─── 群組監控 + 學員管理 + 助教後台
└── IX.   學員頁面 ─── Dashboard + 飲食指南 + 測驗 + 自我覺察
```

### I. 訊息入口
**設計目的**：接住所有訊息，不漏不重複。學員常連發多則，buffer 合併後一次處理。比阿算多了加好友選班流程和群組分流。
**流程**：
```
LINE 事件 → 簽名驗證 → HTTP 200
  ├─ follow（加好友）→ 預載入比對 → 選班 → 姓名確認 → 歡迎    [route.js, user.js]
  ├─ memberLeft → 清除 class_name                              [route.js]
  ├─ 群組訊息 → 精準偵測模式（2026-04-02 重啟）                 [route.js → ai.js]
  └─ 私訊文字 → 班級結業檢查 → buffer 合併（40s）→ 指令偵測 → AI 回覆  [route.js → queue.js]
```
**檔案**：`route.js`（主入口+加好友+指令分流）、`queue.js`（buffer 合併）、`line.js`（LINE API）
**壞了 = 整個 Bot 不回話**

### II. AI 分析
**設計目的**：所有行為由 SYSTEM_PROMPT 驅動。模型按「看的人決定等級」：學員看的用 Haiku，AI 讀的中間產物用 Flash Lite。
**架構**：
```
SYSTEM_PROMPT（~254 行，永遠帶）                               [ai.js]
  ├─ 7 大核心教學理念 + 營養知識 + 一休金句 + 7 種情境指引
  + knowledgeContext（Tier1 + Tier2，由 knowledge.js 組裝）     [knowledge.js]
  + userContext（每次動態組裝）                                  [user.js]
```
**三層模型**：High=`claude-haiku-4.5`（Portrait） / Text=`gemini-3.1-flash-lite`（主對話） / Tags=`gemini-2.5-flash-lite`（意圖/標籤/摘要）
**成本優化**：客套話短路 / 智慧截斷 / 追問輕量化（30min 跳過 journey/summary）/ 同主題重複偵測 / 摘要限頻 / **Context Caching**（SYSTEM_PROMPT 共用 cache，cached tokens 打 25 折，env `CONTEXT_CACHE_ENABLED`）
**檔案**：`ai.js`（SYSTEM_PROMPT + handleMessage + 群組偵測）、`gemini-cache.js`（Context Cache 管理）、`cost-tracker.js`（花費追蹤，含 cached_tokens 折扣）
**詳見**：CLAUDE.md「SYSTEM_PROMPT 核心內容」「成本優化機制」

### III. 知識系統
**設計目的**：課程知識精準注入。不是把所有知識塞進 prompt，而是 AI 判斷意圖後選取最相關的 2 塊。知識太多 = 注意力分散，太少 = 回覆空泛。
**架構**：
```
classifyIntent()（Flash Lite ~200 token, temp 0.1）           [knowledge.js]
  輸入：用戶訊息 + 最近 2 則上下文
  輸出：{ tags, mood, slices }
  → tags 匹配 Tier2 知識塊（最多 2 塊）
  → slices 決定用戶切片注入（identity 永遠 + AI 選 0-2 塊）
  → AI 失敗 → regex 關鍵字匹配降級
```
**Tier1**（永遠注入 ~800 字元）：代謝重建 ABC、胰島素、菜肉飯、蛋白質、好油壞油、外食策略
**Tier2**（AI 選取最多 2 塊，共 21 塊）：膽固醇/聚餐/肌少症/暴食心態/停滯期/隱藏碳水...
**更新流程**：新課程筆記 → `課程知識總結.md` → `knowledge.js` Tier2 → push main
**檔案**：`knowledge.js`
**詳見**：CLAUDE.md「知識注入系統」

### IV. 記憶系統
**設計目的**：小幫手的護城河。「記住你是誰」是差異化核心。跟阿算共用 Redis 實例但 key prefix 不同（`coach-` vs `user:`）。
**架構**：
- **Write-through**：寫 Redis → 非阻塞 sync Supabase
- **Read-through**：Redis miss → Supabase 查詢 → 回寫 Redis（含 AI 重新解析自介）
**檔案**：`user.js`（用戶 CRUD + 自介偵測/解析 + 預載入比對 + 班別 + 目標 + streak + buildUserContext）、`chat.js`（對話歷史）、`supabase.js`（客戶端）
**壞了 = 小幫手失憶**
**詳見**：CLAUDE.md「Redis 資料結構」「Supabase 資料表」「Read-through 回補邏輯」

### V. 用戶智能
**設計目的**：從對話中自動提取標籤，讓小幫手越聊越懂你。累積式旅程摘要是長期陪伴的核心。
**提取管線**（每次 AI 回覆後，非阻塞執行）：
```
AI 回覆
  → extractCoachingTags()    — topic/emotion/core_issue/style/goal_action  [tags.js]
  → saveCoachingTags()       — Redis + Supabase                           [tags.js]
  → shouldUpdateTrend()？    — 每 3 天 + ≥5 次 → updateCoachingSummary()   [tags.js]
  → shouldUpdateJourney()？  — 每 3 天 + ≥5 次 → updateJourneySummary()   [tags.js]
  → checkMilestones()        — 觸發里程碑                                  [tags.js]
  → recordStreak()           — 健康存摺連續天數                             [user.js]
```
**旅程摘要**：累積式（前一版 + 近 20 標籤 + 摘要 + 里程碑 → Flash Lite 更新），500-800 字，注入 buildUserContext
**Read-through 回補**（Redis miss 時）：
```
getRecentTopics()    → coaching_tags 最近 20 則                           [tags.js]
getCoachingSummary() → ≥3 則 → AI 重新產生                               [tags.js]
getJourneySummary()  → users.journey                                     [tags.js]
```
**檔案**：`tags.js`（標籤+摘要+旅程+里程碑）、`user.js`（buildUserContext）

### VI. 群組偵測（⚠️ 暫停中）
**設計目的**：偵測群組裡的學員問題，產草稿讓教練快速回覆。「寧可多抓」策略。
**原有流程**：群組訊息 → AI 偵測（信心 0-1）→ 草稿（走完整 SYSTEM_PROMPT + 知識）→ 通知教練
**檔案**：`ai.js`（aiDetectQuestion + generateDraftResponse）、`pending.js`（待回應管理）、`chat.js`（群組 buffer）
**詳見**：CLAUDE.md「群組偵測系統」

### VII. 推播與遊戲化
**設計目的**：主動觸達 + 遊戲化留存。推太多 = 被封鎖，推太少 = 被遺忘。
**推播 Cron**：
```
週三 08:00  type=weekly       課程週數推播                    [smart-push/route.js]
每天 20:10  type=evening      沉默學員關心                    [smart-push/route.js]
週四 12:15  type=renewal-noon  續報提醒                       [smart-push/route.js]
週六 10:00  type=abc-quiz     ABC 測驗推播                    [smart-push/route.js]
```
**遊戲化**：
- 食物分類測驗：178 題，8 大分類，4 等級收集系統。LINE 對話 + 網頁。`quiz-data.js` + `app/quiz/page.js` + `app/api/quiz/route.js`
- 瘦身知識大挑戰：270 題是非題，零 token。`knowledge-quiz-data.js` + `app/knowledge/page.js` + `app/api/knowledge/route.js`
- 健康存摺：streak 連續天數。`user.js` recordStreak
- 行動目標：對話設定 → AI 追蹤 → Quick Reply 回報。`user.js` + `goals` 表
**詳見**：CLAUDE.md「遊戲化系統」

### VIII-IX
**管理後台**：`app/admin/page.js`（群組監控+匯入）+ `app/admin/students/page.js`（學員管理）+ `app/staff/page.js`（助教）。驗證 = ADMIN_API_KEY
**學員頁面**：`/dashboard`（Portrait+情緒趨勢+進步+收集+目標+里程碑）/ `/guide`（飲食指南 7 類）/ `/quiz` `/knowledge` `/check`
**詳見**：CLAUDE.md「檔案結構」

---

## 核心資料流

> 每個步驟標 `[檔案]`，有疑問直接去讀對應檔案。

### 流程 A：學員傳私訊（最常見路徑）

```
① LINE 事件 → 簽名驗證 → HTTP 200                             [route.js]
② buffer 合併（40s 窗口，防連發拆多次）                          [queue.js]
③ 記憶讀取：getUser(Read-through) + getChatHistory              [user.js, chat.js]
④ 上下文組裝：buildUserContext                                  [user.js]
   (profile + summary + journey + activeGoal)
⑤ 知識選取：classifyIntent → Tier1 + Tier2 + 用戶切片           [knowledge.js]
⑥ AI 回覆：handleMessage                                       [ai.js]
   (SYSTEM_PROMPT + knowledgeContext + userContext + chatHistory)
   → cost-tracker 記帳                                          [cost-tracker.js]
⑦ 回覆：sendMessage(先回用戶) → addChatMessage(再記歷史)         [line.js, chat.js]
⑧ 非阻塞提取：                                                  [tags.js, user.js]
   extractCoachingTags → shouldUpdateTrend → shouldUpdateJourney
   → checkMilestones → recordStreak
⑨ 非阻塞同步：syncUserToSupabase                                [user.js]
```

### 流程 B：加好友（選班 + 預載入比對）

```
① follow 事件 → getProfile                                     [route.js, line.js]
② tryMatchPreloaded()                                          [user.js]
   ├─ 完全比對成功 → 歡迎 + 帶自介
   ├─ 重名 → setPendingVerify → 確認真實姓名                    [user.js]
   ├─ 無比對 → setPendingClassSelect → Quick Reply 選班          [user.js]
   │            └─ 選完班 → 引導自我介紹
   └─ 無預載資料 → 純歡迎 + 引導自介
③ 預載入資料來源：助教後台上傳名單                               [app/staff/page.js]
   → /api/admin/import API → Redis coach-preload:* keys         [app/api/admin/import/route.js]
```

### 流程 C：知識注入（每次對話都走）

```
① 用戶訊息 + 最近 2 則上下文                                    [knowledge.js classifyIntent]
② Gemini Flash Lite 分類（~200 token, temp 0.1）
   → { tags, mood, slices }
③ tags 匹配 Tier2（最多 2 塊）+ slices 決定用戶切片
④ 組裝 knowledgeContext = Tier1（永遠）+ 匹配的 Tier2 + 用戶切片
⑤ AI 失敗 → regex 關鍵字匹配降級（知識塊），用戶切片回退全量
```

### 流程 D：智慧推播

```
Vercel Cron → /api/cron/smart-push?type=evening                [smart-push/route.js]
  → 遍歷有 class_name 的學員                                    [user.js]
  → 檢查冷卻（coach-push-log, 1 天 TTL）
  → 檢查最近互動（conversations count）                          [supabase.js]
  → 沉默 > N 天 → 個人化關心推播 → LINE Push                     [line.js]
```

### 流程 E：記憶同步

```
寫入（Write-through）：                                         [user.js, chat.js]
  Redis 寫入 → 非阻塞 sync Supabase

讀取（Read-through）：                                          [user.js, chat.js, tags.js]
  Redis get → miss → Supabase 查詢 → 回寫 Redis
  特殊：getUser miss 時會觸發 AI 重新解析自介                     [user.js]
```

### 流程 F：Dashboard Portrait

```
① 學員點「我的進步」→ Rich Menu → Dashboard 連結               [route.js]
② /api/dashboard API                                           [app/api/dashboard/route.js]
   → 檢查 Redis coach-portrait:{userId}（14 天快取）
   → 過期或版本不同 → Claude Haiku 生成 Portrait                [app/api/dashboard/route.js]
      讀：coaching_tags + milestones + journey + goals
   → 回傳 Portrait + 情緒趨勢 + 進步紀錄
③ Dashboard 頁面呈現                                           [app/dashboard/page.js]
```

---

## 資料相依（誰讀誰寫）

### users（核心表）

| 欄位 | 寫入 | 讀取 |
|------|------|------|
| id (LINE userId) | Bot 建立 | 全部 |
| display_name | Bot `route.js` (LINE API) | Bot, 後台, 助教後台 |
| intro / info | Bot `user.js` (自介偵測+AI解析) | Bot buildUserContext |
| class_name | 預載入比對 / 助教後台 / Quick Reply 選班 | 推播邏輯, 後台, 助教後台 |
| goal | Bot `user.js` (對話設定) | Bot buildUserContext, Dashboard |
| journey | Bot `tags.js` (旅程摘要) | Bot buildUserContext, Dashboard |
| week_number | 助教後台 | 推播 (課程週數) |
| join_date | Bot 建立時 | 推播 (續報提醒) |

### 其他表

| 表 | 寫入 | 讀取 |
|----|------|------|
| conversations | `chat.js` Write-through | Bot Read-through, 後台對話紀錄 `admin/history` |
| coaching_tags | `tags.js` extractCoachingTags | Bot (摘要/旅程), Dashboard (情緒趨勢), 後台 `admin/students` |
| milestones | `tags.js` checkMilestones | Dashboard |
| goals | `user.js` setGoal/completeGoal | Bot buildUserContext, Dashboard |
| coach_quiz_collected | `app/api/quiz/route.js` | Dashboard (收集進度) |
| coach_quiz_sessions | `app/api/quiz/route.js` | Dashboard, `/quiz/history` |
| coach_knowledge_collected | `app/api/knowledge/route.js` | Dashboard (知識收集進度) |
| coach_knowledge_sessions | `app/api/knowledge/route.js` | Dashboard, `/knowledge/history` |
| abc_self_checks | `app/api/check/route.js` | `/check/history` |
| abc_api_usage | `cost-tracker.js` | — |

---

## 與阿算的關係

| 項目 | 共用 | 各自獨立 |
|------|------|---------|
| Redis 實例 | ✅ 同一個 Upstash | key prefix 不同（`coach-` vs `user:`/`chat:`） |
| Supabase | ✅ 同一個專案 | 表名不同（`users` vs `abc_users`） |
| Gemini API Key | ✅ 共用 | 模型選擇各自決定 |
| abc_api_usage | ✅ 共用（bot 欄位區分） | — |
| abc_self_checks | ✅ 共用 | — |
| LINE OA | ❌ 不同 | — |
| Vercel 部署 | ❌ 不同 project | — |

---

## 索引：有疑問去哪找

### 按任務找

| 我在做... | 先讀指揮中心 | 再讀（細節） |
|-----------|-------------|-------------|
| Bot 新功能 | 流程 A + 架構 V | CLAUDE.md → 對應 lib/*.js |
| 知識注入 | 流程 C + 架構 III | CLAUDE.md「知識注入系統」→ `knowledge.js` |
| 加好友/選班 | 流程 B | → `user.js` tryMatchPreloaded / setPendingClassSelect |
| 推播 | 流程 D + 架構 VII | CLAUDE.md「Cron 排程」→ `smart-push/route.js` |
| 遊戲化（測驗） | 架構 VII | → `quiz-data.js` `knowledge-quiz-data.js` `app/api/quiz/route.js` |
| Dashboard | 流程 F + 資料相依 | → `app/dashboard/page.js` `app/api/dashboard/route.js` |
| 群組偵測 | 架構 VI（暫停中） | CLAUDE.md「群組偵測系統」→ `ai.js` `pending.js` |
| 記憶/同步 | 流程 E + 架構 IV | CLAUDE.md Redis/Supabase 段 → `user.js` `chat.js` |
| 後台/助教 | 架構 VIII | CLAUDE.md「檔案結構」→ `app/admin/` `app/staff/` |

### 按檔案找設計邏輯

| 檔案 | 在指揮中心 | 在 CLAUDE.md |
|------|-----------|-------------|
| `route.js` | 流程 A①② + B① | 「處理流程」「加好友流程」 |
| `ai.js` | 架構 II + VI | 「SYSTEM_PROMPT 核心內容」「群組偵測」 |
| `knowledge.js` | 流程 C + 架構 III | 「知識注入系統」 |
| `user.js` | 流程 A③④ + B② + E | 「Read-through 回補」 |
| `chat.js` | 流程 A⑦ + E | — |
| `tags.js` | 架構 V 提取管線 | — |
| `quiz-data.js` | 架構 VII | 「遊戲化系統」 |
| `knowledge-quiz-data.js` | 架構 VII | 「遊戲化系統」 |
| `smart-push/route.js` | 流程 D | 「Cron 排程」 |
| `app/dashboard/page.js` | 流程 F③ | 「學員 Dashboard」 |
| `app/api/dashboard/route.js` | 流程 F② | 「學員 Dashboard」 |

---

## 影響範圍速查

| 改... | 直接影響 | 連帶影響 |
|------|---------|---------|
| SYSTEM_PROMPT | AI 回覆風格/品質 | 標籤提取品質（AI 輸出格式變 → 提取跟著壞） |
| knowledge.js | 知識注入內容 | 回覆專業度、classifyIntent 的 tags 匹配 |
| tags.js | 標籤/摘要/旅程提取 | 個人化品質、Dashboard 情緒趨勢、推播邏輯 |
| user.js | 用戶資料讀寫 | 幾乎所有檔案 import（最危險的改動對象） |
| chat.js | 對話歷史 | AI 上下文 |
| queue.js | buffer 窗口 | 回覆速度 vs 完整度 |
| quiz-data.js | 食物測驗題庫 | 收集進度、等級系統 |
| smart-push | 推播邏輯 | 用戶留存 / 被封鎖風險 |
| Supabase 表 | 永久層 | 所有讀寫該表的 code |

---

## 最近重大變更

| 日期 | 變更 | 影響 |
|------|------|------|
| 2026-04-04 | Gemini Context Caching 上線（SYSTEM_PROMPT 共用 cache，省 67% input） | AI 分析、成本 |
| 2026-04-04 | 班級結業控制（endDate + 10 天寬限 + graduating 續報引導） | 訊息入口 |
| 2026-04-03 | journey/summary 完全停用，改用即時 coaching_tags 注入 | 用戶智能、AI 分析 |
| 2026-04-02 | 群組偵測重啟（精準模式：≥0.8 + 30min 冷卻 + 帶 buildUserContext 草稿） | 群組偵測 |
| 2026-04-01 | 群組 @回覆誤判自介 fix（looksLikeIntroduction 過濾 @ 開頭） | 記憶系統 |
