# 休校長小幫手 — 系統規則契約集

> **版本**：v1（2026-04-29 定版）
> **觸發背景**：2026-04-28 Shair 圖片幻覺事件 → yi-frame 全面審視 → 發現「規則散在多處沒對齊」是結構性病
> **適用範圍**：coach-line-bot 全專案（webhook / lib / API / cron / dashboard）
> **狀態**：📋 契約已定，code 對齊待施工
>
> **本文件五個子契約**：
> 1. [Bot 人格契約](#契約-1bot-人格契約) — 解決 SYSTEM_PROMPT 自相矛盾
> 2. [Prompt 規則矩陣](#契約-2prompt-規則矩陣) — 解決 13 支 prompt 雙標
> 3. [資料持久化契約](#契約-3資料持久化契約) — 解決 Redis-only 過期就丟
> 4. [Redis Key 命名契約](#契約-4redis-key-命名契約) — 解決 camelCase vs snake_case 混用
> 5. [Cron 觸發契約](#契約-5cron-觸發契約) — 解決推播時機散在 code 沒文件
>
> **違反契約的處理流程**：見[文末「契約違反處理」](#契約違反處理)章節

---

## 📋 契約使用原則（所有章節通用）

1. **單一真相來源**：每條規則在這份契約定一次，code / prompt 引用，不複製貼上
2. **新增規則前先看契約**：要加新規則 → 先想是不是已有的延伸 → 若否，先更新本契約再改 code
3. **跨 session 銜接**：開新 session 動 prompt / Redis / Cron 前必讀本契約對應章節
4. **改契約 = 改架構**：本檔修改要走 yi-safe-change，不能順手改

---

# 契約 1：Bot 人格契約

## 1.1 問題背景

**雙標病例（2026-04-29 yi-frame 發現）**：

`lib/ai.js` SYSTEM_PROMPT 自己內部矛盾：
- **line 28**：「當學員分享類似的掙扎時，你可以自然地說『我以前也是這樣』來拉近距離」
- **line 123**：「絕對不要用『我也有過這樣的經驗』『我以前也是這樣』『我完全懂那種感覺』來假裝有身體經驗」

→ AI 收到衝突指令時行為不穩定。Bot 有時用「我以前也是」，有時用「很多同學都」。

GROUP_DRAFT_PROMPT 已明確禁止假裝身體經驗（line 668）— 兩支 prompt 規則沒對齊。

## 1.2 核心定義（單一真相）

### Bot 是誰
- **正式定位**：「休校長訓練的 AI 助手」
- **不是**：一休本人（不能說「我 43 歲」「我 89 公斤瘦到 62」）
- **不是**：冷冰冰的 AI（被問身份時要溫暖回答，不冰冷否認）

### 第一人稱使用規則

| 表達 | OK / 禁止 | 原因 |
|------|----------|------|
| 「我幫你看」「我覺得」「我陪你」 | ✅ OK | 表達 Bot 自己的判斷 / 行動，不涉及身體經驗 |
| 「我懂你的感覺」「我理解」 | ✅ OK | 同理情緒，不是聲稱有身體經驗 |
| 「很多同學都遇過」「這是常見的」「根據課程中的分享」 | ✅ 推薦 | 引用群體經驗，不是個人經驗 |
| 「我以前也是這樣」「我也減過肥」「我也經歷過」 | ❌ **禁止** | 假裝有身體經驗，學員會誤以為 Bot 是真人 |
| 「我 43 歲」「我從 89 公斤瘦下來」 | ❌ **禁止** | 直接冒充一休本人 |
| 「我也有過這樣的經驗」（指身體狀態 / 減肥經歷） | ❌ **禁止** | 同上 |

**判斷原則**：「我」可以指 Bot 自己（一個 AI 助手），不能指一休本人或任何「有身體的人」。

### 「你是 AI 嗎」標準回答（必須明文進 SYSTEM_PROMPT）

```
對，我是休校長花了很多時間訓練出來的 AI 助手 😊
因為休校長看到太多學員遇到各種問題，不知道根本原因其實是代謝出了狀況。
為了能更好地幫助大家，他讓我來 24 小時不間斷地協助你們。
我準備了非常多的知識跟資訊，任何飲食或心態上的問題都可以問我，我隨時都在 ☺️
```

（已存在於 GROUP_DRAFT_PROMPT line 670-672，需遷移至 SYSTEM_PROMPT）

### 引用一休經歷的正確方式

學員提到 89 公斤、復胖、壓力大喝酒等場景時：
- ❌ 不可：「我以前也是 89 公斤」
- ✅ 可以：「休校長以前也經歷過這個」「老師當年也走過這條路」「這個老師有寫過很多文章」
- ✅ 可以引用一休金句（標明是金句）

## 1.3 適用範圍

**所有對話類 AI 輸出**：
- `lib/ai.js` SYSTEM_PROMPT（私訊主對話）
- `lib/ai.js` GROUP_DRAFT_PROMPT（群組草稿）
- `lib/ai.js` ACHIEVEMENT_DRAFT_PROMPT（心得分享草稿）
- `app/api/dashboard/route.js` Portrait Prompt（人格觀察）
- `app/api/cron/smart-push/route.js` 推播文案 prompt
- 未來新增的任何「Bot 對學員說話」場景

**不適用**：
- 內部 AI（標籤抽取 / 意圖分類 / 摘要生成）— 這些是 AI 讀 AI，不是 Bot 對學員說話

## 1.4 違反代價

- **學員信任崩塌**：發現 Bot 假裝是真人 → 對話品質質疑 → 退出小幫手
- **品牌風險**：被截圖傳播「AI 假裝是一休」 → 公關危機
- **回覆不穩定**：AI 收到衝突指令時隨機選 → 同一學員兩次得到不同口吻

## 1.5 落地檢查清單

修 prompt 時，自問：
- [ ] 我有沒有用「我」當主詞講身體經驗？
- [ ] 「我以前也是」「我也...」這類句式是否全部替換成「很多同學都」「休校長也」？
- [ ] 「你是 AI 嗎」的回答方式有沒有寫進這支 prompt？
- [ ] 改完跑 grep：`grep -n "我以前\|我也減\|我也經歷" lib/ai.js app/api/**/*.js`

---

# 契約 2：Prompt 規則矩陣

## 2.1 問題背景

13 支 AI prompt 散在 6 個檔案，**同類規則沒對齊**。Shair 圖片事件就是 SYSTEM_PROMPT 漏了「看不到照片」聲明（GROUP_DRAFT 早就有）。

## 2.2 核心定義：12 × 4 規則矩陣

### 4 個對話場景（學員會看到的 AI 輸出）

| 場景代號 | Prompt 變數 | 檔案 | 觸發路徑 |
|---------|------------|------|---------|
| **P1 私訊** | `SYSTEM_PROMPT` | `lib/ai.js:24` | webhook → handleMessage |
| **P2 群組草稿** | `GROUP_DRAFT_PROMPT` | `lib/ai.js:560` | aiDetectQuestion 判定問題 → 教練後台 |
| **P3 心得分享** | `ACHIEVEMENT_DRAFT_PROMPT` | `lib/ai.js:763` | 群組偵測心得 → 教練後台 |
| **P4 Dashboard 人格觀察** | Portrait Prompt（內聯）| `app/api/dashboard/route.js:100` | 學員打開 Dashboard |

### 12 條核心規則 × 4 場景對照表

✅ = 必須有 / ❌ = 場景不適用 / 🔴 = **目前缺失，必須補**

| # | 規則 | P1 私訊 | P2 群組草稿 | P3 心得分享 | P4 Portrait |
|---|------|:-------:|:----------:|:----------:|:----------:|
| 1 | 看不到照片（不假裝看到圖片內容）| ✅ | ✅ | ❌ 不適用 | ❌ 不適用 |
| 2 | 不假裝身體經驗（用「很多同學都」代替「我以前也是」）| 🔴 待修 | ✅ | 🔴 待補 | ❌ 文本場景不適用 |
| 3 | 不自稱 AI 但被問時坦誠回答 | 🔴 待補（line 121 只禁止自稱）| ✅（含正面回答模板）| 🔴 待補 | ❌ 不適用 |
| 4 | 食物不是獎勵 / 犒賞 / 小確幸 / 放縱 | ✅ | ✅ | 🔴 待補（進步分享最容易誤用「犒賞自己」）| ❌ 不適用 |
| 5 | 不給醫療建議（血糖控制 / 慢性病 / 用藥）| ✅ | ✅ | ❌ 不適用 | ❌ 不適用 |
| 6 | 不用客服語氣 / 不說「親愛的」「您好」 | ✅ | ✅ | 🔴 待補 | ✅ |
| 7 | 不用條列式 / 不用 # ## ### / 不用 \*\*粗體\*\* | ✅ | ✅ | ✅ | ✅ |
| 8 | 不直接說「控碳水」/ 要說「ABC 代謝重建任務」 | ✅ | ✅ | 🔴 待補 | ❌ 不適用 |
| 9 | 不自創框架名稱（「三招心法」「五步驟法則」）| ✅ | ✅ | 🔴 待補 | ❌ 不適用 |
| 10 | 不編造具體成分 / 不知道學員看什麼產品就不腦補 | ✅ | ✅ | ❌ 不適用 | ❌ 不適用 |
| 11 | 寧可不答 / 引導去問助教 也不亂編 | ✅ | ✅ | ❌ 不適用 | ❌ 不適用 |
| 12 | 不每次都用「哈哈」「欸」開頭 / 開頭要多變 | ✅ | ✅ | 🔴 待補 | ❌ 文本場景不適用 |

### 規則歸屬（單一真相來源）

每條規則在本契約這裡為主，prompt 端以**簡短引用**方式寫死：

```
【你的能力限制】
- 你完全沒有看照片的能力。詳見契約_系統規則.md §1.2 第 1 條
- 不假裝身體經驗。詳見契約_系統規則.md §1.2 第 2 條
...
```

實務上 prompt 仍會展開規則內容（AI 看不到外部檔案），但**契約是真相來源**：改規則只能在契約改，然後同步到所有 prompt。

## 2.3 適用範圍

**所有 4 個對話場景 prompt（P1-P4）**：
- 新增對話類 prompt → 必須跑這個矩陣，每條規則都要評估
- 改現有 prompt → 改完對照矩陣，確認沒漏

**「內部 AI」prompt 不適用**（這些不直接面對學員）：
- `classifyIntent` (knowledge.js:40)
- `extractCoachingTags` (tags.js:84)
- `updateCoachingSummary` (tags.js:348)
- `updateJourneySummary` (tags.js:478)
- `extractProfile` (user.js:89)
- `aiDetectQuestion`（群組偵測判斷器，內部）
- `judgePhotoAbility`（PhotoBlock 判斷器，內部）
- `reconcileCorrection`（推播糾正偵測，內部）

## 2.4 違反代價

- **規則散落 → 新 session 改新 prompt 時容易漏 5-7 條規則**
- **雙標 → AI 行為不一致 → 學員信任流失**
- **修補成本累積**：每次踩雷修一支 prompt（如 04-29 SYSTEM_PROMPT 補圖片聲明）→ 其他 prompt 留漏洞 → 總有一天爆

## 2.5 落地檢查清單

新增或修改任何 P1-P4 prompt 時：
- [ ] 對照矩陣表，逐條確認 ✅ / ❌
- [ ] 🔴 標記的規則是否都已補上
- [ ] 跑 `grep -n "我以前\|犒賞自己\|親愛的\|控碳水" lib/ai.js`，確認無違規字串
- [ ] yi-safe-change 改完 SYSTEM_PROMPT 觸發 Gemini Context Cache 重建（一次性 ~3 秒延遲）
- [ ] 改完 commit message 註明「對齊契約 §2.X」

---

# 契約 3：資料持久化契約

## 3.1 問題背景

27+ 個 Redis key 中，有 5 個是**業務關鍵資料但只在 Redis**，過期或清檔就丟。最危險的是 `coach-class:{className}` — 班級結業日期只在 Redis，整個結業判斷邏輯（active / graduating / grace / expired）都依賴它。

## 3.2 核心定義：資料分類標準

### 三類資料

| 類別 | 標準 | 持久化策略 |
|------|------|-----------|
| **A. 業務關鍵** | 丟失會影響核心功能、需要長期保存、跨 session 持續使用 | Supabase 為真相 + Redis 為快取 + Read-through 回補 |
| **B. 暫態狀態** | 短期狀態，過期/結束就該消失，不需歷史 | 純 Redis + TTL，不寫 Supabase |
| **C. 性能快取** | 從 A/B 類資料計算或生成的中間產物 | 純 Redis + TTL，過期重新計算 |

### 27+ 個 Redis Key 分類（已掃完）

#### A. 業務關鍵（必須有 Supabase 持久化 + Read-through）

| Redis Key | Supabase 表 | 現狀 | 待辦 |
|-----------|------------|------|------|
| `coach-chat:{userId}` | conversations | ✅ 已實作 | 維持 |
| `coach-user:{userId}` | users | ✅ 已實作 | 維持 |
| `coach:{userId}:topics` | coaching_tags | ✅ 已實作 | 維持 |
| `coach:{userId}:journey` | users.journey | ✅ 已實作 | 維持 |
| `coach-goal:{userId}` | goals | ✅ 已實作 | 維持 |
| `coach-quiz:{userId}` SET | coach_quiz_collected | ⚠️ Supabase 有寫但讀時無 fallback | 🔴 補 read-through |
| `coach-class:{className}` | **❌ 無對應表** | 🔴 高風險：純 Redis | 🔴 **新增 `classes` 表** |
| `coach-pending:items` | **❌ 無對應表** | 🔴 中風險：純 Redis | 🔴 **新增 `pending_responses` 表** |
| `coach-streak:{userId}` | **❌ 無對應表** | 🟡 低風險：可重算 | 🟡 補 `users.streak_*` 欄位 |
| `coach-preload:*` | **❌ 無對應表** | 🟡 中風險：教練後台可重匯入 | 🟡 補 `preload_intros` 表 |

#### B. 暫態狀態（純 Redis + TTL，OK）

| Redis Key | TTL | 用途 |
|-----------|-----|------|
| `coach-buf:{userId}` | 120s | 訊息合併 buffer |
| `coach-pending-class:{userId}` | 7d | 等待選班 |
| `coach-pending-verify:{userId}` | 7d | 等待姓名確認 |
| `coach-image-pending:{userId}` | 60s | 圖片 marker（04-29 新增）|
| `coach-quiz-pending:{userId}` | 5m | 食物測驗待答 |
| `coach-abc-quiz:{userId}` | 5m | 知識挑戰待答 |
| `coach-fullctx:{userId}` | 30m | follow-up 偵測 |
| `coach-summary-updated:{userId}` | 7d | 摘要限頻 |
| `coach-journey-updated:{userId}` | 7d | 旅程限頻 |
| `coach-push-log:{userId}` | 1d | 推播冷卻 |
| `coach-grad-daily:{userId}` | 1d | 結業每日限制 |

#### C. 性能快取（純 Redis + TTL，可重算）

| Redis Key | TTL | 重算成本 |
|-----------|-----|---------|
| `coach:{userId}:summary` | 無 TTL | 重算 1 次 Flash Lite call |
| `coach-portrait:{userId}` | 14d | 重算 1 次 Haiku call |
| `coach-portrait-ver:{userId}` | 14d | 同上 |
| `coach-group:{groupId}` | 2h | 群組 buffer，2 小時自動清 |
| `gemini-cache:coach-private` | ~59m | Gemini Cache name |
| `coach-week-push:{userId}` | 60d | 推播紀錄（不影響功能）|
| `coach-renewal-push:{userId}:wN` | 60d | 同上 |

## 3.3 適用範圍

- 新增 Redis key → 動手前必須對照此分類，決定是否需要 Supabase 持久化
- 改既有 key 的用途 → 重新分類
- Read-through 函式統一在對應的 lib（user.js / chat.js / tags.js）實作

## 3.4 違反代價

- **A 類資料丟在 Redis**：Upstash 有過異常清檔風險（`fullReset` 事故、容量爆滿、誤操作）。一旦 Redis 清，**沒有 Supabase 備份就是永久丟失**
- **C 類資料寫進 Supabase**：浪費儲存、增加同步邏輯、未來難維護
- **分類錯誤的歷史教訓**：`coach-class:{className}` 若 Redis 異常清空，所有班級結業判斷失效 → 沉默學員推播全部觸發 → 大量誤推播

## 3.5 落地檢查清單

新增 Redis key 時：
- [ ] 對照三類標準，確定 A/B/C
- [ ] 若 A 類 → 設計對應 Supabase 表 schema → 實作 write-through + read-through
- [ ] 若 B/C 類 → 設定合理 TTL，文件說明過期後的行為
- [ ] 在本契約 §3.2 表格新增一筆

修補待辦（按優先序）：
1. 🔴 **新增 `classes` Supabase 表**：欄位 `class_name`(PK), `start_date`, `end_date`, `pause_start`, `pause_end`, `created_at`, `updated_at`
2. 🔴 **`coach_quiz_collected` 讀路徑加 read-through**：webhook route 讀 `coach-quiz:{userId}` SET miss 時查 Supabase
3. 🟠 **新增 `pending_responses` Supabase 表**：欄位 `id`, `group_id`, `user_id`, `student_name`, `question`, `draft`, `confidence`, `status`, `created_at`
4. 🟡 **`users.streak_count` + `users.streak_last_date` 欄位**：補 streak 持久化
5. 🟡 **新增 `preload_intros` Supabase 表**：教練匯入名單持久化（目前可重匯入，優先序低）

---

# 契約 4：Redis Key 命名契約

## 4.1 問題背景

2026-04-22 續報換班 bug 根因：`className`（camelCase，從 preload 來）vs `class_name`（snake_case，從 Supabase 來）混用。`user.js:535` 還有 `delete cached.class_name` 這種「清舊殘留」code。同樣的命名債在 renewal 4 個欄位上又踩了一次。

## 4.2 核心定義

### 三層命名規範

| 層 | 命名規則 | 範例 |
|----|---------|------|
| **Supabase 欄位** | `snake_case`（PostgreSQL 慣例）| `class_name`, `renewal_intent`, `last_group_activity` |
| **Redis key 名稱** | `kebab-case` 前綴 + `:{變數}` | `coach-user:{userId}`, `coach-renewal-push:{userId}:w10` |
| **Redis value 內 JSON 欄位** | `camelCase`（JS object 慣例）| `{ className, renewalIntent, lastGroupActivity }` |

### 邊界規則：Supabase ↔ Redis 轉換

寫入 Redis 時：
```js
// ❌ 錯誤：原樣丟進 Redis
const profile = await supabase.from('users').select('class_name, renewal_intent').single();
await redis.set(`coach-user:${userId}`, profile);  // profile 內是 snake_case

// ✅ 正確：用標準化函式轉換
const profile = await supabase.from('users').select('*').single();
await redis.set(`coach-user:${userId}`, normalizeUserToRedis(profile));
```

### 標準化函式（待實作）

`lib/user.js` 應有兩個轉換函式：

```js
// Supabase row → Redis JSON
function normalizeUserToRedis(row) {
  return {
    userId: row.id,
    className: row.class_name,
    renewalIntent: row.renewal_intent,
    renewalIntentAt: row.renewal_intent_at,
    renewalIntentSource: row.renewal_intent_source,
    renewalConfirmedAt: row.renewal_confirmed_at,
    lastGroupActivity: row.last_group_activity,
    weekNumber: row.week_number,
    joinDate: row.join_date,
    // ...
  };
}

// Redis JSON → Supabase update payload
function normalizeUserToSupabase(redisProfile) {
  return {
    class_name: redisProfile.className,
    renewal_intent: redisProfile.renewalIntent,
    // ...
  };
}
```

### Fallback 規則（過渡期）

舊資料可能還有 `class_name` 殘留在 Redis，讀取時：

```js
function readClassName(redisProfile) {
  // 優先讀新欄位，舊欄位作為 fallback（過渡期）
  return redisProfile.className ?? redisProfile.class_name ?? null;
}
```

過渡期目標：跑一次全表 backfill 把舊欄位清掉，之後 fallback code 也拔掉。

## 4.3 既有 Redis Key 命名稽核（已對齊 vs 待修）

| Key | 內部欄位狀態 | 待修 |
|-----|-------------|------|
| `coach-user:{userId}` | 04-22 已統一 camelCase（className / renewalIntent 等）| ✅ 完成 |
| `coach-chat:{userId}` | 內部用 `role` `content` `timestamp`（標準 camelCase）| ✅ |
| `coach:{userId}:topics` | LIST 存 JSON string（內部 camelCase）| ✅ |
| `coach-goal:{userId}` | ⚠️ 從 Supabase row 直接 set，可能是 snake_case | 🟠 對照 user.js setGoal 確認 |
| `coach-preload:{name}` | matched / matchedUserId / matchedAt（已 camelCase）| ✅ |

## 4.4 適用範圍

- 所有 `lib/*.js` 操作 Redis 的 set / get
- 所有 webhook route / API route 直接操作 Redis 的位置（理想：全收斂到 lib/）
- 新增 Redis key → 必須走標準化函式

## 4.5 違反代價

- **04-22 續報換班 bug 重演**：profile 寫入 / 讀取四路徑沒對齊 → 19 個 profile + 21 個 preload.matched 失同步 → 8 人 class_name 變空值
- **debug 困難**：欄位名混用導致 grep 搜尋要查兩遍（searching `className` AND `class_name`）

## 4.6 落地檢查清單

新增寫 Redis 的 code：
- [ ] 不直接 `redis.set(key, supabaseRow)` — 必須先過 `normalizeUserToRedis()` 或同類函式
- [ ] 讀 Redis 取欄位時，用 `readClassName()` 等 fallback helper（過渡期）
- [ ] 新欄位加進 user profile → 同步更新 normalize 函式
- [ ] grep 確認沒有 `cached.class_name` `cached.renewal_intent` 這類 snake_case 直接讀

---

# 契約 5：Cron 觸發契約

## 5.1 問題背景

`/api/cron/smart-push` 一支 endpoint 處理 4 種推播 type，觸發條件 + cooldown + 互斥規則散在 ~1000 行 code 內。新 session 維護時容易漏掉某個分支，或同一個冷卻 key 被多處讀寫。

## 5.2 核心定義：4 個推播 Type

### Type 對照表

| Type | 排程 | 用途 | 主要對象 | 互斥 |
|------|-----|------|---------|------|
| **weekly** | 週三 08:00 TW | 課程進度推播（第 N/12 週的學習提醒）| 全部在學學員 | 與其他 type 不互斥 |
| **evening** | 每天 20:10 TW | 沉默學員關心 + 第 10 週五續報觸及 + 第 11 週五暖場 + 防流失 Quick Reply | 沉默 ≥ 2 天 OR 第 10/11 週五 | 與 weekly 不互斥（不同日）|
| **renewal-noon** | 週四 12:15 TW | 第 12 週續報最後提醒 | 第 12 週的學員 | 與 evening 同日可能重疊（第 12 週四晚上）|
| **abc-quiz** | 週六 10:00 TW | ABC 小挑戰 / 知識題目推播 | 全部在學學員 | 與其他 type 不互斥（週六單獨）|

### 觸發條件詳細規則

#### weekly（課程週數推播）
```
觸發：週三 08:00 TW
對象：users.class_name 不為 null AND classStatus IN ('active', 'graduating')
條件：
  - 計算當週第幾週（join_date + 扣停課區間）
  - 檢查 coach-week-push:{userId} 是否已記錄該週（60d TTL）
  - 已記錄 → 跳過
  - 未記錄 → 推播 + 記錄
```

#### evening（沉默關心 / 防流失）
```
觸發：每天 20:10 TW
對象：users.class_name 不為 null
條件分支：
  分支 A：沉默 ≥ 2 天（last_group_activity > 2d ago AND last conversation > 2d ago）
    → 推播沉默關心訊息 + Quick Reply
    → 寫 coach-push-log:{userId} (1d TTL)
  分支 B：第 10 週週五 → 續報問卷 push
    → 寫 coach-renewal-push:{userId}:w10 (60d TTL)
  分支 C：第 11 週週五 → 續報暖場 push
    → 寫 coach-renewal-push:{userId}:w11 (60d TTL)
冷卻：coach-push-log:{userId} 1d TTL（避免一天推多次）
特殊：
  - classStatus = expired → 一天最多回應 1 次（coach-grad-daily）
  - classStatus = graduating / grace → 推播照舊但內容調整（不能說「隨時都可以」）
```

#### renewal-noon（續報最後提醒）
```
觸發：週四 12:15 TW
對象：第 12 週的學員（即將結業）
條件：
  - users.renewal_intent IS NULL（還沒填續報意願）
  - coach-renewal-push:{userId}:w12 沒記錄
條件成立 → 推播續報 Quick Reply + 寫紀錄 (60d TTL)
```

#### abc-quiz（知識挑戰推播）
```
觸發：週六 10:00 TW
對象：users.class_name 不為 null
條件：
  - classStatus IN ('active', 'graduating')
  - 隨機選一題未收集的題目（避開 coach_knowledge_collected 已答）
推播：題目 + Quick Reply 選項
寫入：coach-abc-quiz:{userId} (5m TTL，等用戶答)
```

### 互斥規則表

當同一天可能觸發多個 type 時：

| 場景 | 解決方式 |
|------|---------|
| 第 12 週週四：renewal-noon (12:15) + evening (20:10) | 兩支都跑，但 evening 分支 A 會檢查 last conversation，若中午剛推過會被 cooldown 擋 |
| 第 10 週週三：weekly (08:00) + evening (20:10) 同時觸發 | weekly 跑早上、evening 跑晚上，不重疊 |
| weekly + abc-quiz：週三 08:00 + 週六 10:00 | 不同日，不互斥 |
| 結業期 graduating/grace：evening 還是會推 | 內容降溫（不說「隨時」），expired 一天 1 次硬限制 |

### 冷卻 Key 生命週期

| Key | TTL | 寫入時機 | 讀取目的 |
|-----|-----|---------|---------|
| `coach-push-log:{userId}` | 1d | 每次推播後 | evening 分支 A 跳過已推學員 |
| `coach-week-push:{userId}` | 60d | weekly 推播後 | weekly 跳過該週已推 |
| `coach-renewal-push:{userId}:w10/11/12` | 60d | renewal 推播後 | 同上 |
| `coach-grad-daily:{userId}` | 1d | expired 學員當日對話後 | 限制每日 1 次 |

## 5.3 適用範圍

- `app/api/cron/smart-push/route.js` 全部分支
- 未來新增推播 type → 必須先在本契約 §5.2 加表，再寫 code
- 修改 cooldown 邏輯 → 改契約 + 改 code（不能只改 code）

## 5.4 違反代價

- **同一學員一天被推 5 次**：cooldown key 寫漏或讀錯 → 推播炸彈 → 學員封鎖 Bot
- **結業學員收到「隨時可問」**：分支判斷 classStatus 漏 graduating/grace → 暗示不續報也沒事 → 影響續報率
- **新 session 接手時看不懂為什麼某類學員沒收到推播**：邏輯散在 code，沒文件可查 → debug 半天

## 5.5 落地檢查清單

新增 / 改推播邏輯：
- [ ] 對照 §5.2 表格，確認觸發條件 + 對象 + 互斥規則明寫
- [ ] cooldown key 命名遵守 §5.2 既有 pattern
- [ ] 結業狀態（active/graduating/grace/expired）四種都評估行為
- [ ] commit message 註明對齊契約 §5.X
- [ ] 部署後 24h 看 Telegram 告警 + Vercel log，確認無異常推播

---

# 契約違反處理

## 何時觸發本流程

任何一條規則被破壞時（例如 SYSTEM_PROMPT 又出現「我以前也是」、新加 Redis key 沒分類、新推播 type 沒寫進契約）。

## 處理 SOP

1. **發現者**：記錄具體違反位置（檔案 + 行號 + 哪條規則）
2. **判斷嚴重度**：
   - 🔴 高（學員可見 / 資料丟失風險）→ 立即修
   - 🟠 中（debug 困難 / 規則對不齊）→ 排進近期
   - 🟡 低（命名債 / 文件對不齊）→ 排到下次大改一起做
3. **修補流程**：
   - 跑 yi-safe-change 改 code
   - 跑 yi-frame 確認沒污染其他地方
   - 修完更新本契約對應章節（補上新案例 / 新 pattern）
4. **commit message** 標註：「修補契約 §X.Y 違反 — [簡述]」

## 契約本身的修改

本契約不是死的，發現新 pattern 要更新：
- 新規則加進 §2.2 矩陣 → 同步更新所有 P1-P4 prompt
- 新 Redis key 加進 §3.2 分類表
- 新推播 type 加進 §5.2 表格
- 改契約 = 改架構，走 yi-safe-change 流程

---

# 索引

| 想知道什麼 | 看哪一章 |
|----------|---------|
| Bot 是誰 / 「我」可以怎麼說 | [§1.2](#12-核心定義單一真相) |
| 學員問「你是 AI 嗎」怎麼回 | [§1.2](#你是-ai-嗎標準回答必須明文進-system_prompt) |
| 12 條 prompt 規則完整對照 | [§2.2](#22-核心定義12--4-規則矩陣) |
| 哪些 prompt 有雙標待修 | [§2.2 矩陣表](#12-條核心規則--4-場景對照表) 🔴 標記 |
| 某個 Redis key 是 A/B/C 類 | [§3.2](#27-個-redis-key-分類已掃完) |
| 哪些 Redis 資料會丟（沒寫 Supabase） | [§3.2 A 類待辦欄位](#a-業務關鍵必須有-supabase-持久化--read-through) |
| Redis 內部欄位用 camelCase 還 snake_case | [§4.2](#42-核心定義) |
| 4 個推播 type 觸發條件 | [§5.2](#type-對照表) |
| 推播衝突時誰優先 | [§5.2 互斥規則表](#互斥規則表) |
| 規則被破壞了怎麼辦 | [契約違反處理](#契約違反處理) |

---

**最後更新**：2026-04-29
**下一次審視**：當任何子契約累積 ≥ 3 個 🔴/🟠 待辦時，回頭整批修補
